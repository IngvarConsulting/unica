---
id: INV.SAFETY.DAEMON-ENDPOINT-OWNERSHIP
check:
  - crates/unica-coder/src/infrastructure/daemon/client_v5.rs::failed_startup_cleanup_never_adopts_successor_with_reused_pid
gap: https://github.com/IngvarConsulting/unica/issues/967
---

# Запись подключения демона доступна владельцу и удаляется только им

Каталог и файл подключения демона доступны только владельцу. Для подключения
создаётся случайный токен. Завершающийся демон удаляет только свою запись:
замена её записью другого владельца, даже с тем же PID, не даёт права удаления.

Проверка подтверждает сохранение записи преемника при очистке неудачного
запуска. Проверка прав доступа и очистки при завершении простоя требует
отдельного сценария; случайность токена определяется способом его создания,
а не статистическим тестом.
