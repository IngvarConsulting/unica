// Пока не вызывается production-кодом: провайдер справки подключит парсер в
// одной из следующих задач плана. `tests_support` внутри модуля намеренно
// виден только тестам — своим и соседних модулей — и в production не попадёт.
#[allow(dead_code)]
pub mod container;
