# Unica v0.13 Completion Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Завершить миграцию Unica с опубликованной поверхности v0.12.3 на один локальный MCP `unica` с private daemon, восемью предметными входами, native/compatibility Tasks profiles и атомарным RC cutover без сохранения legacy aliases.

**Architecture:** До cutover production остаётся на V12. PR #631 стабилизирует и сливает hidden V13 foundation. После S1 три workers собирают schema-derived/behavior evidence v0.12.3 и классифицируют upstream-влияние на skills, пока integrator закрывает W2a-core. Затем integrator последовательно собирает content-addressed capability baseline и immutable provenance scope review и получает два независимых review; только этот join освобождает три непересекающиеся worker-линии W1. Параллельно integrator завершает W2a-seams до первого перехода любой линии в W3 и непрерывно выполняет W2b wiring. W4 превращает review в content-addressed cutover patch lock. Отдельный RC PR атомарно переключает package-selected surface, skill routing, bootstrap verification и архитектурные evidence records.

**Tech Stack:** Rust 2021, `rmcp 3.1.2`, Tokio, Serde/JSON, UUID v4, `fs2`, существующие platform facade и atomic publication primitives, Python 3.12 CI probes, GitHub Actions, пакеты Unica для Codex и Claude Code.

**Spec:** `docs/design/2026-08-28-v0-13-completion-wavefront-design.md`

## Global Constraints

- Источник baseline — опубликованный `v0.12.3`: 74 уникальных публичных имени, включая шесть `unica.runtime.job.*`. Текущие 71 имя в ledger `main` не заменяют release fixture.
- До Gate G6 package-selected production surface остаётся V12. Hidden V13 доступен только через уже существующие injected constructors и тестовые harnesses; новый CLI flag, environment switch или secret package mode запрещён.
- Единственный публичный MCP-сервер сохраняет identity `unica` и local stdio transport. Daemon остаётся private per-user mode того же versioned core.
- Целевой native Tasks profile содержит ровно восемь entry points: `view`, `apply`, `find`, `search`, `check`, `diff`, `run`, `docs`. Compatibility profile добавляет только `task.get`, `task.result`, `task.cancel`.
- Один предметный вызов создаёт одну Invocation и имеет ровно одно исполнение. Frontend не делает fallback, replay или retry предметной операции после handoff.
- Known-long operation materializes durable Task сразу. Остальные операции завершаются напрямую либо имеют durable receipt к абсолютной границе 7 000 мс от приёма `tools/call`; тесты используют fake clock.
- После receipt прогресс v0.13 наблюдается polling-ом status и `updatedAt`. Новый public progress/log API и generic resume protocol не создаются. После restart продолжение допустимо только для зарегистрированного typed owner; иначе Task получает terminal outcome без повторного исполнения mutation.
- Engines доставляются лениво из `unica-toolchain` по immutable identity; `prefetch` наполняет offline closure. `tools-download` и raw CLI passthrough не входят в `run`.
- `apply` сначала строит полный staged postimage и validation result, затем публикует Source и WorkspaceCache как закрытый transaction set. Ambient filesystem reads после actor admission запрещены.
- Domain layer не импортирует infrastructure. Типы validation target/finding/failure принадлежат domain; read port принадлежит application; persisted и staged adapters принадлежат infrastructure.
- Любой дефект сначала воспроизводится падающим тестом. Увеличение test/CI timeout не считается исправлением daemon hang или неверной ownership boundary.
- Любой новый Rust test filter сначала выполняется с `-- --list`; ноль найденных тестов является RED infrastructure failure, а не зелёным результатом slice.
- Planned `DEC.2026-08-28.V0-13-MIGRATION-EVIDENCE-GATE` владеет общей semantic/provenance release policy. До реализации обоих validators он не является действующим правилом; W4 создаёт active successor и derived checkable records в том же PR.
- Изменение публичной поверхности, wire identity, task observation, packaging или границы слоёв вносит successor `DEC.*` и derived `INV.*`/`CTR.*` в том же PR. Body уже merged product record не меняется; у superseded predecessor обновляются только lifecycle-поля `status` и `superseded-by`.
- `tests`, `features`, `log`, gRPC и platform 8.5 profile остаются вне v0.13.
- Публикация tag, prerelease, stable assets и marketplace promotion требует отдельного owner approval по `docs/release-runbook.md`; этот план не даёт такого разрешения.

## PR Topology and Dependency Graph

```text
PR #631 / W0 hidden foundation
          |
          v
       merged main (S1)
          +-------------------------------+
          v                               v
 W0.5a worker evidence +           W2a-core request router +
 upstream classification            indexed batch/effects
          +---------------+---------------+
                          |
                          v
             W0.5b integrator consolidation +
             baseline/scope locks + independent reviews
                          |
          +----------------------+----------------------+------------------+
          v                      v                      v                  v
     W1A -> W3A             W1B -> W3B*            W1C -> W3C       W2a-seams
          \                      |                      /
           +------ continuous W2b apply wiring --------+
                                  |
                         * B8 waits final W2b
                                  |
             +--------------------+--------------------+
                           v
                 W4/W5 aggregate parity (S2)
                           |
                           v
                 G6 staged RC (pre-S3)
                           |
                           v
                 published RC matrix (S3)
                           |
                           v
                    G7 stable (S4)
```

- W0 changes are committed to the existing PR #631 head branch. A defect introduced by #631 is fixed there, not in a child PR.
- Workers may prepare local branches from an accepted W0 SHA, but no PR may use the open PR head as its base. After #631 merges, every W1/W3 PR starts from current `main`.
- W0.5a and W2a-core start from merged S1 in parallel. Their completion is followed by integrator-owned W0.5b consolidation and independent oracle/provenance reviews; this serial join is not hidden inside the parallel maximum. No W1 worker PR starts until the reviewed v0.12.3 capability oracle/upstream scope lock and the request-level router, indexed-operation SPI and final-effect reconciliation are green. W2a-seams starts from updated `main` immediately after W2a-core and must merge before the first W3 slice; it may overlap the remaining W0.5a evidence, independent-review wait time and W1, but not integrator-owned W0.5b consolidation.
- W1A, W1B and W1C are three parallel lanes, not three mega-PRs. Each lane is a sequence of A1–A6, B1–B6 or C1–C6 short non-stacked PRs; W2b may integrate a slice only after that PR is merged into `main` and cannot finish before all eighteen slices are integrated.
- W2b is an incremental loop of short integrator PRs from updated `main`, not one long-lived branch accumulating all W1 work. A shared delta merges after the corresponding worker slice; the final W2b closure PR starts fresh after all prior worker/integrator PRs are in `main`.
- A worker starts its W3 lane as soon as its own W1 lane is complete and W2a-seams is on `main`; it does not wait for the other two W1 lanes. B8 alone waits for final W2b apply integration. Stable daemon adapters remain integrator-owned.
- W4 mapping/tests may merge before cutover, but the 73 skill content patches remain unapplied until G6 because shipped instructions must continue matching production V12.
- S2 is a revocable readiness claim, not a release. Any later upstream drift or newly classified bundled-tool/product behavior gap invalidates S2 and returns work to the owning toolchain/W1/W3 slice plus W4/W5 revalidation; G6 cannot proceed on stale evidence.
- G6 is one atomic RC cutover PR. G7 is a separate version-only stable PR after the RC host matrix.
- A slice lives no more than two working days without integration review. Long-lived divergent worker branches and stacked PRs are stop conditions.

## Ownership Contract

Four active slots are used: one integrator and three workers. Only the integrator edits these shared hotspots:

```text
crates/unica-coder/src/application/v13/mod.rs
crates/unica-coder/src/application/v13/apply.rs
crates/unica-coder/src/application/validation.rs
crates/unica-coder/src/domain/apply.rs
crates/unica-coder/src/domain/validation.rs
crates/unica-coder/src/infrastructure/native_operations/apply.rs
crates/unica-coder/src/infrastructure/native_operations/apply_families/mod.rs
crates/unica-coder/src/infrastructure/native_operations.rs
crates/unica-coder/src/infrastructure/workspace_actor.rs
crates/unica-coder/src/infrastructure/daemon/mod.rs
crates/unica-coder/src/infrastructure/daemon/server.rs
crates/unica-coder/src/infrastructure/daemon/v13_service.rs
crates/unica-coder/src/interfaces/mcp.rs
crates/unica-coder/src/interfaces/task_projection.rs
crates/unica-coder/src/application/mod.rs
crates/unica-coder/src/application/tool_contracts.rs
crates/unica-coder/src/domain/mod.rs
crates/unica-coder/src/infrastructure/mod.rs
plugins/unica/.mcp.json
plugins/unica/.codex-plugin/plugin.json
plugins/unica/.claude-plugin/plugin.json
arch/index.md
```

Workers own only their family implementation, isolated handler, fixtures and focused tests. When a worker needs a shared seam, the handoff to integrator contains:

1. a minimal failing fixture;
2. the exact expected type/signature;
3. a focused RED command;
4. no edit to a shared hotspot.

`crates/unica-coder/src/infrastructure/native_operations/xdto.rs` has explicit time-phased ownership: integrator owns only its W2a-core authority/router adaptation; after that PR merges and until B4 merges, Worker B exclusively owns the existing XDTO parser/planner section. No integrator PR may touch that file concurrently with B4.

W0 bounds the family planner types. W2a-core freezes the request-level crate-private
SPI before fan-out:

```text
ApplyRequest + ApplyOp + OperationRegistry
ApplyStagedState + ProvisionalApplyEffects + PlannedApplyEffects + ApplyPlanError
parse_<family>_plan_operation(...)
IndexedPlanOperation<T> { request_index, operation }
ProvisionalApplyEffect { event, touched_paths }
plan_<family>_batch(
    ApplyStagedState,
    actor-issued <Family>ApplyAuthority,
    &[IndexedPlanOperation<FamilyPlanOperation>],
) -> Result<(ApplyStagedState, ProvisionalApplyEffects), ApplyPlanError>
finalize_request_effects(&ApplyStagedState, ProvisionalApplyEffects)
    -> PlannedApplyEffects
```

## Baseline, Gates and Estimate

| Gate | Exit evidence | Calendar estimate with 4 slots |
| --- | --- | ---: |
| W0 / S1 | #631 full-lifecycle receipt/cancel fixed; green macOS, Linux, Windows; V12 unchanged | 4–7 days |
| W0.5 | schema-derived v0.12.3 oracle and immutable 41-skill provenance scope review | 6–9 days; evidence collection overlaps W2a-core, consolidation does not |
| W2a-core | indexed request router and final effects | 1–2 days, from the existing W2 integration budget |
| W2a-seams | compiled W3 facades before first W3 slice | 1–2 days, overlaps W1 |
| W1 + W2b | measured `apply` registry closed; real daemon handler | 10–14 days |
| W3 | `search/docs`, `check/diff`, `run` vertical slices | 8–11 days, overlaps completed W1 lanes |
| W4 | execute semantic parity, exact 8/11 profiles, prepare content-addressed skill patches | 4–6 days, overlaps W1/W3 |
| W5 / S2 | hidden V13 aggregate gate and independent review | 5–8 days |
| G6 / pre-S3 | merged staged `0.13.0-rc.1` cutover; no publication claim | 2–4 days |
| RC / S3 | immutable RC assets publish exact 8/11 and pass three-host matrix | 3–7 days, including a minimum 3-day soak |
| G7 / S4 | version-only stable PR and owner-approved promotion | 1–3 days after soak |

The person-day work breakdown is resource-loaded rather than obtained by dividing by four:

| Work package | Person-days |
| --- | ---: |
| W0 stabilization/receipt/cancellation | 6–10 |
| W0.5 semantic oracle/provenance scope lock | 18–27 |
| W1A metadata/property | 9–14 |
| W1B form/resource | 7–10 |
| W1C DCS/MXL | 9–13 |
| W2a/W2b integration | 4–6 |
| W3A search/docs | 5–8 |
| W3B check/diff | 6–10 |
| W3C run | 6–10 |
| W4 parity execution and skill patch preparation | 7–11 |
| W5 hidden integration/package readiness | 9–14 |
| G6 staged RC cutover | 4–6 |
| RC/G7 host validation and release engineering | 4–9 |
| **Total** | **94–148** |

At full fan-out, Workers A/B/C occupy the three family/entry lanes while integrator continuously handles W2a-seams/W2b/W4. Immediately after S1, the workers characterize baseline capabilities and classify their assigned skill entries while the integrator completes W2a-core. The integrator then owns a short sequential consolidation/review tail before W1. During W0, W5 and release gates those slots intentionally become independent reviewers, host/package operators or idle approval capacity; forcing four concurrent code writers there would widen shared hotspots. W2a-seams finishes in parallel before W3. A realistic resource-loaded calendar is now 8–12 weeks: variant-level oracle/provenance evidence, W5, G6, published RC proof and the minimum three-day soak retain sequential portions. Six weeks is no longer a planning commitment.

Estimate confidence is medium-low until the 41 currently affected skill entries are classified by live provenance review. Prose-only routing stays inside the W4 range; any required behavior or bundled-tool port returns to W1/W3 and triggers an explicit re-estimate instead of being absorbed invisibly.

The sequential critical path is W0 → max(W0.5a worker evidence, W2a-core) → W0.5b integrator consolidation and independent reviews → max(each W1→W3 lane, W2a-seams before the first W3) with incremental W2b and the B8 join → W4 aggregate/W5 → G6 → RC publication and host soak → G7. W4 preparation overlaps W1 and W3 but its aggregate gate cannot bypass them.

Before Python checks, use the pinned CI environment:

```bash
uv venv --python 3.12 .build/v013-plan-venv
uv pip install --python .build/v013-plan-venv/bin/python -r tests/ci/requirements.txt
source .build/v013-plan-venv/bin/activate
python3 -c 'import lxml, yaml, tree_sitter, tree_sitter_rust'
```

---

## Task 1: Stabilize PR #631 and Bound the Hidden Foundation (W0)

**Owner:** Integrator

**Files:**

- Modify: `crates/unica-coder/src/infrastructure/native_operations/xdto/writer.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/xdto.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace_actor.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform/process.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/server.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/v13_service.rs`
- Create: `crates/unica-coder/src/infrastructure/native_operations/apply_families/mod.rs`
- Create: `crates/unica-coder/src/infrastructure/native_operations/apply_families/metadata.rs`
- Create: `crates/unica-coder/src/infrastructure/native_operations/apply_families/form_resource.rs`
- Create: `crates/unica-coder/src/infrastructure/native_operations/apply_families/dcs_mxl.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply.rs`
- Modify: `arch/decisions/2026-08-27-retained-source-selection-evidence-slice.md`
- Modify if a normative successor is required: `arch/decisions/`
- Modify if and only if a reproduced runner defect requires it: `.github/workflows/unica-plugin-release.yml`

- [ ] Capture the current RED evidence without editing code:

```bash
cargo fmt --all -- --check
.build/v013-plan-venv/bin/python -m unittest discover -s tests/arch -p 'test_*.py' -v
cargo test -p unica-coder daemon_ -- --nocapture --test-threads=1
gh pr checks 631
```

Expected evidence includes the XDTO/workspace formatting diff, the 51-line product-record guardrail failure, Windows `E0308` at `process_tree.attach(child.child())`, and Linux daemon actor timeouts/hang. Record the exact first failing daemon test rather than treating the 60-minute job cancellation as the defect.

- [ ] Preserve the existing Windows-only behavioral test `process_tree_keeps_child_suspended_until_attach` as the RED evidence: the current Windows compile fails before it can execute because `ProcessTree::attach` receives `&Child`. Do not add a second unit test inside the non-compiling crate. Introduce `ChildCleanupGuard::child_mut(&mut self) -> &mut Child`, bind the guard as mutable, pass `child_mut()` to `ProcessTree::attach`, and run that existing test on Windows.

- [ ] Run formatting and prove no unrelated file changed:

```bash
cargo fmt --all
git diff --name-only
cargo fmt --all -- --check
```

- [ ] Compact `arch/decisions/2026-08-27-retained-source-selection-evidence-slice.md` to at most 40 non-empty lines without changing its decision, status, realization evidence or derived records. This product record was introduced by the still-unmerged #631; do not use this exception for an already accepted record.

- [ ] Reproduce the Linux daemon failures with the narrowest test filter, use fake clock/process lifecycle evidence to locate the shared cause, and add or retain a test that fails for that cause. Fix ownership/lifecycle at the source. Do not increase the test or workflow timeout.

- [ ] Add barrier-controlled RED tests for the complete receipt path, not only executor time after preparation: hold workspace admission and `CanonicalInvocationService::prepare` beyond the handoff boundary; before releasing either barrier prove that exactly one durable Task already exists, and with a still-open transport prove its exact TaskId is returned by the original submission. A late release after the response cutoff must not create an undiscoverable orphan Task. Move validation/admission/preparation under one executor-owned Invocation lifecycle so Task identity and cancellation exist before any potentially blocking stage. A manual clock advanced synchronously inside `prepare()` is not evidence for this concurrency contract.

- [ ] Bridge MCP `notifications/cancelled` into that same pre-receipt Invocation capability. Add RED races for cancellation during admission, during preparation, after durable handoff and against terminal completion; every race reaches one cancellation token, performs no replay and leaves no inaccessible working Task. If protocol v3 cannot identify and cancel the pre-receipt Invocation, change the private protocol/CoreIdentity and its decision/contract in #631 rather than weakening `INV.WIRE.COOPERATIVE-CANCELLATION` or the seven-second handoff invariant.

- [ ] Before declaring S1, exercise the same lifecycle through the injected direct path, native Task projection and compatibility Task projection. The W0 aggregate must prove known-long immediate receipt, the compatibility bounded-wait cutoff, cancellation before and after receipt, and restart reconciliation of every persisted nonterminal phase. These are foundation evidence, not work that may first appear in W4.

- [ ] Add a RED restart test for every persisted nonterminal phase: a valid ownerless `Queued` record must become the same closed interrupted terminal outcome as `Working`, or the two-phase `create`/persisted `Queued` state must be removed. Reopen may not preserve a non-expiring, non-evictable Task with no live owner; prove capacity becomes reclaimable without replay.

- [ ] Reproduce Windows run `33146773991` with focused `--nocapture` filters. Treat the three daemon failures and the `0xc0000409 STATUS_STACK_BUFFER_OVERRUN` in `stale_local_worker_retains_process_until_later_terminal_proof` as separate RED evidence until a shared cause is proved. Fix resource/thread/handle ownership; do not mask the crash by increasing timeouts, skipping Windows or removing the aggregate runtime contract.

- [ ] Resolve the protocol v2/v3 contradiction so the current registry has one daemon wire identity. If a merged product decision must change, create a successor `DEC.*` and derived record; do not edit the accepted product record.

- [ ] Bound the family planner types and validation layer direction stated in this plan. Add compile-time/unit assertions that domain has no infrastructure dependency and that a family planner receives actor-issued authority plus staged state. Do not call the request router frozen until Task 2B proves indexed batches and final-effect reconciliation.

- [ ] Register a compiled family seam: `native_operations.rs` declares `apply_families`; its `mod.rs` declares all three lane modules; each lane file contains a typed stub and a named seam test. The outer module may be explicitly dormant while V12 is selected, but `cargo test ... -- --list` must show at least one lane test. Current singleton routing is provisional evidence only: no W1 worker merge is allowed before Task 2B replaces it with the request-level indexed router.

- [ ] Prove that default stdio still selects V12 and the public ledger has not changed:

```bash
cargo test --workspace -- --test-threads=1
.build/v013-plan-venv/bin/python tests/ci/test_tool_surface_ledger.py -v
.build/v013-plan-venv/bin/python -m unittest discover -s tests/arch -p 'test_*.py' -v
python3 scripts/arch/registry.py --check
python3 scripts/arch/immutability.py --base origin/main
```

- [ ] Push the fixes to the existing #631 head and require green macOS, Linux and Windows checks plus independent semantic review before merge. Exit state is S1; it is not a v0.13 user release.

## Task 2: Create the Continuous Migration Parity Inventory (W0)

**Owner:** Integrator

**Files:**

- Create: `tests/fixtures/v013/domain-parity/view-find.json`
- Create: `tests/fixtures/v013/domain-parity/apply-metadata.json`
- Create: `tests/fixtures/v013/domain-parity/apply-form-resource.json`
- Create: `tests/fixtures/v013/domain-parity/apply-dcs-mxl.json`
- Create: `tests/fixtures/v013/domain-parity/check-diff.json`
- Create: `tests/fixtures/v013/domain-parity/search-docs.json`
- Create: `tests/fixtures/v013/domain-parity/run.json`
- Create: `tests/ci/test_v013_parity_inventory.py`

- [ ] Write a failing Python test that separates baseline name accounting, legacy capability variants, genuinely new V13 capabilities and executable parity. Every baseline row has one unique `legacyTool` from the immutable 74-name `tests/fixtures/migration/v0.12.3-baseline.json` plus a non-empty unique `variants[]`; each variant is keyed by non-empty `legacyVariant` and has one of `mapped`, `absorbed`, `transport-replaced`, `removed`. This is deliberately one-to-many: `unica.runtime.execute`, `meta.edit`, `dcs.edit` and similar V12 tools contain several capabilities and may map their variants to different `(entry, operation)` identities. A V13 capability without any legacy predecessor lives only in `newCapabilities[]` with a unique `capabilityId`, successor, cases and rationale; it must never be assigned to an unrelated legacy name merely to make coverage green. Do not substitute the current tool ledger for the release baseline. An executable case has a globally unique `caseId`, an entry from the exact eight-entry set, an exact `operation` for `apply` or `run`, explicit non-empty domain-specific `mode`, fixture path and non-empty object `expected`. The generic inventory validates structure; each implementation slice owns the vocabulary and typed result shape for its modes and expected values.

```json
{
  "schemaVersion": 2,
  "complete": false,
  "baselineDispositions": [
    {
      "legacyTool": "unica.runtime.execute",
      "variants": [
        {
          "legacyVariant": "operation=config-init",
          "disposition": "mapped",
          "successor": {"entry": "run", "operation": "source.create"},
          "caseIds": ["runtime-config-init"]
        },
        {
          "legacyVariant": "operation=syntax",
          "disposition": "mapped",
          "successor": {"entry": "run", "operation": "syntax.check"},
          "caseIds": ["runtime-syntax"]
        }
      ]
    },
    {
      "legacyTool": "unica.runtime.job.status",
      "variants": [
        {
          "legacyVariant": "default",
          "disposition": "transport-replaced",
          "projections": [
            {"kind": "native-task", "method": "tasks/get"},
            {"kind": "compatibility-tool", "tool": "unica.task.get"}
          ]
        }
      ]
    }
  ],
  "cases": [
    {
      "caseId": "runtime-config-init",
      "entry": "run",
      "operation": "source.create",
      "mode": "fast",
      "fixture": "tests/fixtures/v013/run/runtime-config-init.json",
      "expected": {"outcome": "created"}
    },
    {
      "caseId": "runtime-syntax",
      "entry": "run",
      "operation": "syntax.check",
      "mode": "fast",
      "fixture": "tests/fixtures/v013/run/runtime-syntax.json",
      "expected": {"outcome": "checked"}
    },
    {
      "caseId": "run-query-new",
      "entry": "run",
      "operation": "query.execute",
      "mode": "fast",
      "fixture": "tests/fixtures/v013/run/query-execute.json",
      "expected": {"outcome": "executed"}
    }
  ],
  "newCapabilities": [
    {
      "capabilityId": "run.query.execute",
      "successor": {"entry": "run", "operation": "query.execute"},
      "caseIds": ["run-query-new"],
      "rationale": "No v0.12.3 public capability executes a typed query"
    }
  ]
}
```

- [ ] Make the seven JSON files valid inventories with exactly the top-level keys `schemaVersion`, `complete`, `baselineDispositions`, `cases` and `newCapabilities`. `schemaVersion` is integer `2` (not boolean), `complete` is boolean, and the other three values are arrays. Reject duplicate JSON keys at any nesting level and reject extra keys. At W0 the files have `complete: false` and empty arrays; later incomplete slices may add rows, variants, cases and new capabilities, so the repository smoke test must not permanently require empty arrays.

- [ ] Validate exact row and variant forms. A baseline row has exactly `legacyTool`, `variants`; variants are non-empty and their `legacyVariant` values are unique inside the tool. `mapped` and `absorbed` variants have exactly `legacyVariant`, `disposition`, `successor`, `caseIds`; `caseIds` is a non-empty unique string array. Each referenced case exists, has the exact same `(entry, operation)` identity as the variant successor and is owned by no other legacy variant or new capability. `successor` has exact key `entry`, requires a non-empty `operation` only for `apply` or `run`, and forbids it otherwise. A `run` operation must be one of the exact 13 typed dictionary values from Task 9. `transport-replaced` has exactly `legacyVariant`, `disposition`, and a non-empty `projections` array; each unique projection is exactly `{"kind":"native-task","method":"tasks/get|tasks/cancel"}` or `{"kind":"compatibility-tool","tool":"unica.task.get|unica.task.result|unica.task.cancel"}`. `removed` has exactly `legacyVariant`, `disposition`, and non-empty string `rejectionEvidence`; mutually exclusive successor/projection fields are invalid. A new capability has exactly non-empty unique `capabilityId`, `successor`, non-empty unique `caseIds` and non-empty `rationale`.

- [ ] Validate every case as exactly `caseId`, `entry`, optional-by-entry `operation`, `mode`, `fixture`, `expected`: `operation` is required for `apply` and `run` and forbidden for the other six entries. Fixture paths use canonical repository-relative POSIX spelling and must resolve to tracked regular non-symlink files below the repository root; reject backslashes, drive/UNC or absolute paths, empty/`.`/`..` components, non-normalized paths, escape, missing paths, directories, a final or ancestor symlink, and untracked paths. Obtain tracked paths from NUL-delimited `git ls-files -z` and pass repository root, immutable baseline names and that explicit set into the pure validator.

- [ ] Treat `complete` as one distributed **structural** aggregate gate. Mixed values are invalid; W0 and every partial slice keep all seven values false. W4 alone changes all seven to true, and only after all 74 immutable baseline names (including the exact six `unica.runtime.job.*` names) have one row, Task 2A proves every oracle variant accounted, all eight native entries and every one of the exact 13 `run` operations have executable cases, every `mapped`/`absorbed` variant and every genuinely new capability owns at least one exact case, and no executable case is unowned. Variant-level/new-capability `caseIds` carry the specific `apply` or `run` identity, so one representative case cannot stand in for a family tool. `complete: true` proves JSON closure only: it is necessary but never sufficient for S2. Semantic equivalence additionally requires the independently captured Task 2A oracle and Task 10 Rust runner. Live apply-operation parity remains Task 10 Rust/`OperationRegistry::closed()` evidence, not a duplicated Python registry.

- [ ] Run:

```bash
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
```

- [ ] Commit the inventory skeleton in #631. Every later slice must add its rows and executable fixtures in the same PR as its implementation.

## Task 2A: Lock Legacy Semantics and Upstream Scope (W0.5)

**Owners:** Workers A/B/C collect disjoint evidence; integrator alone owns the aggregate oracle, provenance review and scope decision

**Files:**

- Create: `tests/fixtures/migration/v0.12.3-tools-list.json`
- Create: `tests/fixtures/migration/v0.12.3-tools-list.provenance.json`
- Create: `tests/fixtures/migration/v0.12.3-capability-catalog.json`
- Create: `tests/fixtures/migration/v0.12.3-capability-oracle.json`
- Create: `tests/ci/test_v0123_capability_catalog.py`
- Create: `tests/ci/test_v013_capability_oracle.py`
- Create: `docs/provenance/reviews/2026-08-28-v013-skill-routing-upstream-review.json`
- Prepare but do not merge before G6: a reviewed delta for `docs/provenance/skill-upstreams.json`

- [ ] From the immutable published v0.12.3 package/tag, capture the raw `tools/list` response before authoring the oracle. Keep that file as payload only. A separate tracked provenance sidecar records package/tag identity, exact asset checksum, capture command and SHA-256 of the raw payload; it never hashes an envelope containing its own digest and is never regenerated from current V13 code. Reject a sidecar whose declared payload digest differs from the raw bytes.

- [ ] Deterministically derive `v0.12.3-capability-catalog.json` from that snapshot. The extractor walks every tool and treats a schema alternative as a variant dimension only when its discriminator selects different externally observable behavior; ordinary value enums inside one behavior are not blindly cross-multiplied. Every behavior discriminator records its schema JSON pointer plus the exact immutable v0.12.3 handler branch and executable probe that justify the split; `oneOf`/`anyOf`, `if`/`then`/`else`, optional selectors and reachable combinations are all closed explicitly. An ungrounded or open selector blocks W0.5. The generated catalog is content-addressed and hand-editing it without a matching snapshot/extractor change is rejected.

- [ ] Capture a reviewed capability oracle for every identity in the generated catalog. Oracle identity is `(legacyTool, legacyVariant)`, not just the tool name: one legacy tool may own several allowed typed successors/projections/removals. Every mapped/absorbed oracle case contains the exact legacy request, immutable fixture identity, normalized legacy result or side effect, expected V13 `(entry, operation)` and a stable `caseId`. Capabilities that truly have no V12 predecessor are listed separately as `newCapabilities`; they cannot be attached to an unrelated legacy variant. Record reviewer and evidence; self-declared V13 output is not a legacy oracle.

- [ ] Add a failing catalog validator that reproduces the extraction from the raw snapshot and requires byte-equivalent identities and selector evidence. Then add a separate oracle validator requiring exact set equality `schema-derived catalog == oracle legacy identities`: no missing/extra variant, no duplicate identity, and no `newCapabilities` entry in the legacy catalog. It also rejects a shard disposition or `(entry, operation)` that differs from its reviewed oracle, an unbound mapped/absorbed case, non-executable transport/removal evidence, an oracle case with no immutable legacy observation, a falsely declared new capability that has a V12 predecessor, and any oracle reference not consumed by exactly one parity variant. These checks are distinct from `test_v013_parity_inventory.py`, which intentionally proves shape only.

- [ ] Split the 74-name characterization across the three workers by the same domain lanes used later in W1/W3. The integrator reviews and commits the single aggregate oracle; workers never concurrently edit it. A case that cannot be normalized or whose successor is ambiguous is a scope blocker, not permission to choose a convenient entry.

- [ ] In parallel, capture live upstream drift with `scripts/ci/check-skill-upstreams.py --check --format json` and `--prepare-upstream-review`. Classify all currently affected entries, including the observed 41-skill/602-path `cc-1c-skills` delta at target `7409bacd47846dc96b66a9f943fa990224abbc6e`. The integrator commits one immutable review with `reviewStatus: reviewed`, per-entry decision/evidence and no unresolved `needs-review`/`needs-tool-update`; workers only supply their assigned evidence.

- [ ] Classify every required change as routing/prose, bundled-tool, or product behavior. Routing/prose work proceeds to Task 11; bundled-tool work follows immutable toolchain acquisition; behavior/API gaps are inserted into the owning W1/W3 lane now. Recalculate the resource-loaded range before fan-out when either latter class is non-empty. Do not create the former W4 → W1/W3 feedback cycle.

- [ ] Freeze the immutable review, 41 per-entry scope decisions and expected index dispositions without applying any content change to the V12-selected package. Task 11 later turns this approved scope into tracked patch/index bytes and a complete G6 manifest. A live check may legitimately continue to report drift before G6; `affectedEntries: []` and `upstreamDrift: false` become post-application G6 evidence.

- [ ] Run:

```bash
.build/v013-plan-venv/bin/python tests/ci/test_v0123_capability_catalog.py -v
.build/v013-plan-venv/bin/python tests/ci/test_v013_capability_oracle.py -v
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
python3 scripts/ci/check-skill-upstreams.py --validate-only --format json
```

- [ ] Exit W0.5 only after an independent migration reviewer approves the oracle and an independent provenance reviewer approves the 41-entry classification. W1 fan-out waits for both W0.5 and W2a-core; W2a-core may run in parallel.

## Task 2B: Freeze the Request-Level Apply Router (W2a-core)

**Owner:** Integrator, first PR from merged S1 and before any W1 worker merge

**Files:**

- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply_families/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply_families/metadata.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply_families/form_resource.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply_families/dcs_mxl.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/code.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/event.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/xdto.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace_actor.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/v13_service.rs`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: shared request-router tests

- [ ] Treat planned `DEC.2026-08-28.REQUEST-LEVEL-APPLY-EFFECT-RECONCILIATION` as the accepted direction, not an active rule. In the W2a implementation PR create its active successor/evidence plus `INV.APP.REQUEST-LEVEL-APPLY-EFFECT-RECONCILIATION` with the named aggregate check; preserve the merged predecessor body and update only its lifecycle frontmatter to `status: superseded` plus `superseded-by`.

- [ ] Add RED request-level tests before changing the router. Prove that two inverse XDTO operations leave zero planned changes and zero effects; an interleaved three-family request preserves exact global order; a planner failure reports the original global `ops[i]`; a transient first duplicate cannot suppress a later surviving event; a poisoned final operation publishes neither Source nor WorkspaceCache; and XDTO, Code and Event reject a foreign authority or staged state.

- [ ] Introduce the integrator-owned `IndexedPlanOperation<T>` wrapper. Parsers and planners preserve `request_index`; no family planner may reconstruct an index by enumerating its local slice.

- [ ] Replace per-operation singleton dispatch with one request-level planning pass over the ordered operation stream. Family delegation may use ordered contiguous runs, but it returns path-bound `ProvisionalApplyEffects`; only the request-level finalizer may convert candidates to `PlannedApplyEffects` against the final staged postimage. A `DomainEvent` without the touched-path evidence cannot prove add→remove restoration, so blindly appending or locally finalizing intermediate effects is forbidden. Finalization first removes candidates whose touched paths have no final change, then deduplicates the surviving events in stable first-surviving-occurrence order as selected by planned `DEC.2026-08-28.REQUEST-LEVEL-APPLY-EFFECT-RECONCILIATION`. `DEC.2026-08-26.RETAINED-APPLY-EFFECT-PUBLICATION-SLICE` governs publication only after finalization and must not be stretched into the missing planner policy.

- [ ] Route XDTO through `ApplyAdmission::xdto_planning_authority()` and the existing `parse_xdto_plan_operation`/`plan_xdto_batch`; B4 then owns integration hardening and parity, not a second XDTO writer implementation. Route Code through `CodeApplyAuthority`. Add an admission-sealed Event authority before connecting `plan_event_implement_batch`; raw source-set/profile arguments are not an authority boundary.

- [ ] Run:

```bash
cargo test -p unica-coder request_level_apply_router -- --nocapture
cargo test -p unica-coder staged_xdto -- --nocapture
cargo test -p unica-coder apply -- --nocapture
```

- [ ] Merge W2a-core only after independent apply/actor review and re-prove that production V12 `tools/list` is unchanged. Start Task 2C from that updated `main`; start the three W1 lanes only after Task 2A is also approved.

## Task 2C: Compile the W3 Facades (W2a-seams)

**Owner:** Integrator, first PR from `main` after W2a-core; may overlap W1 but must merge before any W3 slice

**Files:**

- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Create: `crates/unica-coder/src/application/v13/search.rs`
- Create: `crates/unica-coder/src/application/v13/docs.rs`
- Create: `crates/unica-coder/src/application/v13/check.rs`
- Create: `crates/unica-coder/src/application/v13/diff.rs`
- Create: `crates/unica-coder/src/application/v13/run.rs`
- Create: `crates/unica-coder/src/application/validation.rs`
- Modify: `crates/unica-coder/src/domain/mod.rs`
- Create: `crates/unica-coder/src/domain/validation.rs`
- Create: `crates/unica-coder/src/domain/structural_diff.rs`
- Create: `crates/unica-coder/src/domain/run.rs`
- Modify: `crates/unica-coder/src/infrastructure/mod.rs`
- Create: `crates/unica-coder/src/infrastructure/v13_validation.rs`
- Create: `crates/unica-coder/src/infrastructure/v13_diff.rs`
- Create: `crates/unica-coder/src/infrastructure/v13_run.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/v13_service.rs`
- Modify: shared seam/daemon reachability tests

- [ ] Add RED compile/reachability tests for all five missing entry modules and the shared validation/diff/run adapters. Register typed stub adapters in the canonical daemon service with stable `provider_unavailable` behavior; no worker may need a late edit to a shared facade.

- [ ] Create the complete integrator-owned `ValidationView`/validator-registry seam, typed structural-diff port and exact 13-operation run adapter boundary. Domain types import no infrastructure; infrastructure adapters receive actor-issued capabilities.

- [ ] Run:

```bash
cargo test -p unica-coder v13_ -- --list
cargo test -p unica-coder validation_ -- --nocapture
cargo test -p unica-coder daemon_ -- --nocapture
```

- [ ] Merge from current `main` after independent layer-boundary review. W1 may continue concurrently, but W3 cannot start until this PR is on `main` and every named filter lists at least one test.

## Task 3: Close Metadata and Property Apply Operations (W1A)

**Owner:** Worker A after both W0.5 reviews and W2a-core merge

**Files:**

- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply_families/metadata.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/meta/edit.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/meta/help_facet.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/meta/predefined.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/meta/template_catalog.rs`
- Modify: metadata family focused tests and `tests/fixtures/v013/domain-parity/apply-metadata.json`

The measured lane contains these 34 operations:

```text
object.create object.remove props.set relation.add relation.remove relation.replace
help.create
attribute.add attribute.set attribute.remove
tabularSection.add tabularSection.set tabularSection.remove
dimension.add dimension.set dimension.remove
resource.add resource.set resource.remove
enumValue.add enumValue.set enumValue.remove
column.add column.set column.remove
template.add template.set template.remove
command.add command.set command.remove
predefinedItem.add predefinedItem.set predefinedItem.remove
```

Mergeable sub-slices, each limited to at most two working days and one non-stacked PR:

| Slice | Operations |
| --- | --- |
| A1 | `object.create/remove`, `props.set`, `relation.add/remove/replace`, `help.create` |
| A2 | `attribute.add/set/remove`, `tabularSection.add/set/remove` |
| A3 | `dimension.add/set/remove`, `resource.add/set/remove` |
| A4 | `enumValue.add/set/remove`, `column.add/set/remove` |
| A5 | `template.add/set/remove`, `command.add/set/remove` |
| A6 | `predefinedItem.add/set/remove` |

- [ ] For each sub-slice, add RED parser fixtures for every included operation name, exact logical target kind, required arguments and unknown-field rejection. Assert that no operation expands `tools/list`.

- [ ] Add RED batch tests for deterministic op order, `ifRev`, dry-run/real parity, duplicate target conflict, poisoned second op rollback, byte/BOM/EOL preservation and parity with the existing native writer.

- [ ] Implement family-local parse types and `plan_metadata_batch(...)` using only the frozen SPI. The planner returns staged postimage plus path-bound `ProvisionalApplyEffects`; only the W2a request-level finalizer may produce `PlannedApplyEffects`. The family planner does not publish files or mutate cache.

- [ ] Add one parity-inventory row per legacy metadata/property tool disposition and executable fixtures for successful, invalid and rollback paths.

- [ ] Run focused tests, then the full apply/parity set:

```bash
cargo test -p unica-coder metadata_apply -- --nocapture
cargo test -p unica-coder apply -- --nocapture
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
```

- [ ] For each A1–A6 row, open an independent PR from current `main`, obtain spec and quality review, merge it, then recreate the next branch from updated `main`. Do not edit shared hotspots and do not base one slice on an open predecessor.

## Task 4: Close Form, Role, XDTO, Subsystem and Support Apply Operations (W1B)

**Owner:** Worker B after both W0.5 reviews and W2a-core merge

**Files:**

- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply_families/form_resource.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/form.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/form_event_registry.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/role.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/subsystem.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/support.rs`
- Modify in B4 only after the W2a-core ownership transfer: `crates/unica-coder/src/infrastructure/native_operations/xdto.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/xdto/writer.rs`
- Modify: family focused tests and `tests/fixtures/v013/domain-parity/apply-form-resource.json`

The measured lane contains these 23 operations:

```text
form.add form.set form.remove form.create
element.add element.remove formAttribute.add formCommand.add event.bind
role.create right.set
valueType.add objectType.add property.add type.remove property.remove
subsystem.create content.add content.remove childSubsystem.add childSubsystem.remove
supportCapability.set supportRule.set
```

Mergeable sub-slices, each limited to at most two working days and one non-stacked PR:

| Slice | Operations |
| --- | --- |
| B1 | `form.add/set/remove/create` |
| B2 | `element.add/remove`, `formAttribute.add`, `formCommand.add`, `event.bind` |
| B3 | `role.create`, `right.set` |
| B4 | integrate/harden existing XDTO batch planner: `valueType.add`, `objectType.add`, `property.add`, `type.remove`, `property.remove` |
| B5 | `subsystem.create`, `content.add/remove`, `childSubsystem.add/remove` |
| B6 | `supportCapability.set`, `supportRule.set` |

- [ ] For each sub-slice, add RED parser fixtures for all included names, target-kind validation, required argument validation and rejected ambiguous targets.

- [ ] Add RED transaction tests for ordered postimage, `ifRev`, dry-run/real parity, poison rollback, byte/BOM/EOL preservation and parity with the existing form/role/XDTO/subsystem/support writers.

- [ ] Implement `plan_form_resource_batch(...)` for Form, Role, Subsystem and Support through the frozen SPI and return staged postimage plus path-bound `ProvisionalApplyEffects`; family code never produces `PlannedApplyEffects`. Read all required state through the supplied staged snapshot/authority; do not reopen a path after actor admission. XDTO stays on the separate W2a-core `XdtoApplyAuthority` route: B4 receives exclusive ownership of the existing `parse_xdto_plan_operation`/`plan_xdto_batch` section after W2a-core, adds parity and hardening there, and must not move it behind `FormResourceApplyAuthority`.

- [ ] Extend parity inventory with every affected legacy tool and executable success/failure/rollback fixtures.

- [ ] Run:

```bash
cargo test -p unica-coder form_resource_apply -- --nocapture
cargo test -p unica-coder apply -- --nocapture
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
```

- [ ] For each B1–B6 row, open an independent PR from current `main`, obtain spec and quality review, merge it, then recreate the next branch from updated `main`. Do not edit shared hotspots and do not base one slice on an open predecessor.

## Task 5: Close DCS and MXL Apply Operations (W1C)

**Owner:** Worker C after both W0.5 reviews and W2a-core merge

**Files:**

- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply_families/dcs_mxl.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/dcs.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/mxl.rs`
- Modify: family focused tests and `tests/fixtures/v013/domain-parity/apply-dcs-mxl.json`

The measured lane contains these 36 operations:

```text
dcs.set
field.add field.set field.remove fieldRole.set
total.add total.remove
calculatedField.add calculatedField.remove
parameter.add parameter.set parameter.rename parameter.reorder parameter.remove
filter.add filter.set filter.remove filter.clear
dataParameter.add dataParameter.set
query.set query.patch
selection.add selection.clear
order.add order.clear
conditionalAppearance.add conditionalAppearance.clear
dataSetLink.add dataSet.add variant.add drilldown.add
outputParameter.set structure.set structure.patch
mxl.set
```

Mergeable sub-slices, each limited to at most two working days and one non-stacked PR:

| Slice | Operations |
| --- | --- |
| C1 | `dcs.set`, `field.add/set/remove`, `fieldRole.set`, `total.add/remove` |
| C2 | `calculatedField.add/remove`, `parameter.add/set/rename/reorder/remove` |
| C3 | `filter.add/set/remove/clear`, `dataParameter.add/set` |
| C4 | `query.set/patch`, `selection.add/clear`, `order.add/clear` |
| C5 | `conditionalAppearance.add/clear`, `dataSetLink.add`, `dataSet.add`, `variant.add`, `drilldown.add` |
| C6 | `outputParameter.set`, `structure.set/patch`, `mxl.set` |

- [ ] For each sub-slice, add RED parser fixtures for all included names and reject operations whose address kind does not match the DCS/MXL target.

- [ ] Add RED transaction tests for exact operation order, multi-op dependency, `ifRev`, dry-run/real parity, poison rollback, byte/BOM/EOL preservation and parity with existing DCS/MXL writers.

- [ ] Implement `plan_dcs_mxl_batch(...)` through the frozen SPI and return staged postimage plus path-bound `ProvisionalApplyEffects`, without publication, cache mutation or family-local finalization.

- [ ] Extend parity inventory with every affected legacy tool and executable success/failure/rollback fixtures.

- [ ] Run:

```bash
cargo test -p unica-coder dcs_mxl_apply -- --nocapture
cargo test -p unica-coder apply -- --nocapture
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
```

- [ ] For each C1–C6 row, open an independent PR from current `main`, obtain spec and quality review, merge it, then recreate the next branch from updated `main`. Do not edit shared hotspots and do not base one slice on an open predecessor.

## Task 6: Integrate the Closed Apply Registry and Real Daemon Handler (W2b)

**Owner:** Integrator

**Files:**

- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply_families/mod.rs`
- Modify: `crates/unica-coder/src/application/v13/apply.rs`
- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: `crates/unica-coder/src/application/validation.rs`
- Modify: `crates/unica-coder/src/domain/apply.rs`
- Modify: `crates/unica-coder/src/domain/mod.rs`
- Modify: `crates/unica-coder/src/domain/validation.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations/apply.rs`
- Modify: `crates/unica-coder/src/infrastructure/native_operations.rs`
- Modify: `crates/unica-coder/src/infrastructure/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/v13_validation.rs`
- Modify: `crates/unica-coder/src/infrastructure/workspace_actor.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/v13_service.rs`
- Modify: cross-family apply/daemon tests

- [ ] Add a failing invariant test proving `OperationRegistry::closed()` is the single source used by `view.can[]`, apply parse and apply dispatch. The test must fail on missing, duplicate, parse-only or dispatch-only operations.

- [ ] Treat W2b as an incremental integrator loop, not a late barrier. After every A/B/C sub-slice merges into `main`, run the registry/parity/cross-family gate from updated `main`; if shared test/wiring changes are required, merge a short integrator PR from that `main` before the next sub-slice in the same lane. Never integrate a commit that exists only on an open worker branch.

- [ ] Add a failing cross-family batch fixture containing one operation from each W1 lane plus an already accepted `code.insert` or `event.implement`. Prove exact order and all-or-nothing rollback when the last operation is poisoned.

- [ ] Wire the accepted family modules behind the registry and the W2a-core indexed request router. Do not duplicate a second operation-name table in daemon or MCP layers, and do not restore singleton dispatch.

- [ ] Route apply through actor-issued authority and `ApplyStagedState`; publish Source and WorkspaceCache only after all planners and validators succeed.

- [ ] Replace the hidden canonical service stub with the real apply handler. Assert one Invocation, one actor admission, one execution and identical direct/Task terminal `DomainResult`.

- [ ] Keep every W2a-seams adapter compiled while W2b evolves. Workers modify only the already registered lane files, so every merged slice remains reachable through daemon wire; `cargo test <lane-filter> -- --list` returning zero tests is a gate failure. Any need for a late shared facade edit returns to integrator review instead of being hidden in a worker PR.

- [ ] Prove the current measured inventory: 96 unique accepted names composed of 34 + 23 + 36 newly closed operations plus accepted `code.insert`, `code.replace`, `event.implement`. Treat this as implementation evidence, not a permanent public limit.

- [ ] Run:

```bash
cargo test -p unica-coder operation_registry -- --nocapture
cargo test -p unica-coder apply -- --nocapture
cargo test -p unica-coder daemon_ -- --nocapture
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
```

- [ ] The final W2b PR starts from `main` after all W1 sub-slices and any earlier wiring PRs are merged. Merge it only after no accepted registry name reaches an unsupported stub, the real daemon apply handler is green and cross-family review passes. Re-prove that production V12 `tools/list` is unchanged.

## Task 7: Implement Search and Documentation Vertical Slices (W3A)

**Owner:** Worker A immediately after A1–A6; W2b may continue for lanes B/C

**Files:**

- Modify: `crates/unica-coder/src/application/v13/search.rs`
- Modify: `crates/unica-coder/src/application/v13/docs.rs`
- Modify: `crates/unica-coder/src/application/documentation.rs`
- Modify: `crates/unica-coder/src/domain/documentation.rs`
- Modify: `crates/unica-coder/src/infrastructure/code_intelligence.rs`
- Modify: `crates/unica-coder/src/infrastructure/documentation_retrieval.rs`
- Modify: `crates/unica-coder/src/infrastructure/standards_documentation.rs`
- Modify: isolated handler tests and `tests/fixtures/v013/domain-parity/search-docs.json`

Mergeable sub-slices, each limited to at most two working days and one non-stacked PR:

| Slice | Scope |
| --- | --- |
| A7 | `search` fast/literal/regex contract, logical scope and provider-neutral addresses |
| A8 | `search` controlled-long indexing/delivery, cancellation and Task parity |
| A9 | `docs` fast retrieval, source-kind filtering and deterministic citations |
| A10 | `docs` controlled-long delivery, SharedWork and offline-prefetched parity |

- [ ] Add failing `search` tests for logical scope, literal/regex modes, stable canonical addresses, provider-neutral results, bounded output and cold controlled-long Task delivery/indexing.

- [ ] Add failing `docs` tests for source-kind filtering, deterministic citations, no provider identity in public result and exact SharedWork behavior when a documentation engine must be delivered.

- [ ] Implement application handlers by reusing domain code-intelligence/documentation policies and infrastructure adapters. Do not place engine delivery in public `run` and do not add a ninth tool.

- [ ] Fill the search/docs parity inventory with explicit dispositions for the associated legacy names and direct/Task expected results.

- [ ] Run focused tests and prove one execution across projections:

```bash
cargo test -p unica-coder v13_search -- --nocapture
cargo test -p unica-coder v13_docs -- --nocapture
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
```

- [ ] For each A7–A10 row, hand its isolated handler delta and daemon-registration RED test to integrator, open an independent PR from current `main`, merge it, then recreate the next branch from updated `main`. Do not edit shared hotspots or base a slice on an open predecessor.

## Task 8: Implement Shared Validation and Structural Diff (W3B)

**Owner:** Worker B consumes the W2a validation seam after B1–B6; B8 alone waits for final W2b

**Files:**

- Read integrator seam: `crates/unica-coder/src/domain/validation.rs`
- Read integrator seam: `crates/unica-coder/src/application/validation.rs`
- Modify: `crates/unica-coder/src/domain/structural_diff.rs`
- Modify: `crates/unica-coder/src/application/v13/check.rs`
- Modify: `crates/unica-coder/src/application/v13/diff.rs`
- Modify: `crates/unica-coder/src/infrastructure/v13_validation.rs`
- Modify: `crates/unica-coder/src/infrastructure/v13_diff.rs`
- Modify: focused tests and `tests/fixtures/v013/domain-parity/check-diff.json`

Mergeable sub-slices, each limited to at most two working days and one non-stacked PR:

| Slice | Scope |
| --- | --- |
| B7 | persisted `check`, stable finding identity/order and actor snapshot |
| B8 | staged `apply(dryRun)` adapter and equality with the shared validator registry |
| B9 | typed structural `diff`, incomparable kinds and zero-write proof |
| B10 | two-revision cursor, stable ordering, limit and pagination |

- [ ] Consume the already merged W2a validation seam; Worker B must not edit either integrator-owned file. The accepted port is:

```rust
pub(crate) trait ValidationView {
    fn revision(&self) -> &SourceRevision;
    fn read(
        &self,
        at: &QualifiedAddress,
    ) -> Result<Option<ValidationNode>, ValidationReadError>;
}
```

Domain owns `ValidationTarget`, `ValidationFinding` and `ValidationFailure`; it must not name `ApplyStagedState` or any infrastructure type.

- [ ] Add failing validation tests proving `check` uses the persisted actor snapshot while `apply(dryRun=true)` uses a staged-state adapter over the same validator registry and yields the same finding identity/order.

- [ ] Add failing `diff` tests for typed structural changes, incomparable node kinds, read-only behavior, stable ordering, limit/cursor and a cursor authenticated to both left and right revisions.

- [ ] Implement persisted and staged `ValidationView` adapters in infrastructure. Validators receive a view and never open the filesystem themselves.

- [ ] Implement structural diff over typed logical nodes, not text-file patches. Assert zero publication/cache mutation for every diff path.

- [ ] Fill parity inventory for legacy validation/diff tools and shared dry-run cases.

- [ ] Run:

```bash
cargo test -p unica-coder validation -- --nocapture
cargo test -p unica-coder structural_diff -- --nocapture
cargo test -p unica-coder apply -- --nocapture
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
```

- [ ] Start B7 after B1–B6. B9 and B10 may proceed from their normal prerequisites while W2b closes other lanes; B8 starts only after final W2b because it proves staged `apply(dryRun)` equality. For each row, hand its isolated handler delta and daemon-registration RED test to integrator, open an independent PR from current `main`, merge it, then recreate the next branch from updated `main`. Do not edit shared hotspots or base a slice on an open predecessor.

## Task 9: Implement the Typed Run Dictionary and Runtime Ownership (W3C)

**Owner:** Worker C immediately after C1–C6; W2b may continue for lanes A/B

**Files:**

- Modify: `crates/unica-coder/src/domain/run.rs`
- Modify: `crates/unica-coder/src/application/v13/run.rs`
- Modify: `crates/unica-coder/src/infrastructure/v13_run.rs`
- Modify: `crates/unica-coder/src/infrastructure/runtime_jobs.rs`
- Modify: `crates/unica-coder/src/infrastructure/platform/process.rs`
- Modify: isolated handler tests and `tests/fixtures/v013/domain-parity/run.json`

The exact v0.13 dictionary is:

```text
source.create source.attach
infobase.create infobase.build
source.dump source.convert
artifact.make artifact.load
syntax.check test.run client.run extension.sync query.execute
```

Mergeable sub-slices, each limited to at most two working days and one non-stacked PR:

| Slice | Scope |
| --- | --- |
| C7 | dictionary plus `source.create/attach/dump/convert` and `artifact.make/load` |
| C8 | `infobase.create/build`, `syntax.check`, `extension.sync`, `query.execute` |
| C9 | `test.run`, terminal `client.run`, process-tree/cancel/restart semantics |

- [ ] Add failing dictionary tests proving `run()` with no `op` returns typed skeletons as data, and proving raw command lines, tool download, legacy job IDs and arbitrary executable passthrough are rejected.

- [ ] Add failing lifecycle tests for known-long immediate receipt, process-tree ownership, cancellation, terminal client behavior and restart terminalization. Task 9 does not add a persisted resume descriptor or registered resume owner: every interrupted v0.13 runtime Invocation receives a deterministic terminal outcome without replay. If real continuation becomes necessary, stop the slice and first create a successor decision plus replacement persistence/recovery records in the same PR.

- [ ] Adapt existing runtime jobs/process facade behind the 13 typed operations. Each operation maps to exactly one runtime owner and one Invocation; no legacy `runtime.job.*` handler is called from V13.

- [ ] Fill the run parity inventory, explicitly disposing all six `unica.runtime.job.*` baseline names without aliases. Every `mapped` or `absorbed` successor names one exact 13-entry `run` operation and is cross-linked to an executable case for that same operation.

- [ ] Run:

```bash
cargo test -p unica-coder v13_run -- --nocapture
cargo test -p unica-coder runtime_resource_tree -- --nocapture
cargo test -p unica-coder task_recovery -- --nocapture
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
```

- [ ] For each C7–C9 row, hand its isolated handler delta and daemon-registration RED test to integrator, open an independent PR from current `main`, merge it, then recreate the next branch from updated `main`. Do not edit shared hotspots or base a slice on an open predecessor.

## Task 10: Close Eight-Handler and Task/Profile Parity (W4)

**Owner:** Integrator

**Files:**

- Modify: `crates/unica-coder/src/application/v13/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/v13_service.rs`
- Modify: `crates/unica-coder/src/interfaces/mcp.rs`
- Modify: `crates/unica-coder/src/interfaces/task_projection.rs`
- Create: `tests/ci/test_v013_surface_profiles.py`
- Modify: all seven `tests/fixtures/v013/domain-parity/*.json`
- Create at implementation evidence time: successor record under `arch/decisions/`
- Modify at implementation evidence time: `arch/contracts/CTR.APP.DAEMON-LONG-WORK-CAPABILITIES.md`
- Generate: `arch/index.md`

- [ ] Retain the already-green exact-list tests in `application/v13/tool_catalog.rs` and `interfaces/mcp.rs`; do not manufacture a duplicate RED for the already-proved 8/11 lists. Add a new RED aggregate requiring zero remaining `provider_unavailable` stubs, reachability of all eight real handlers and one execution across direct, native Task and compatibility Task projection paths.

- [ ] After every W3 sub-slice merges, run its daemon-wire gate against updated `main`; W2a-seams already registered the stable adapters, so worker code must be reachable without a late module/dispatcher edit. After the last slice, remove every remaining `provider_unavailable` stub and assert that all eight handlers produce the same canonical result type and one execution for direct, native Task and compatibility Task projections.

- [ ] Retain and rerun the W0 barrier/fake-clock receipt, compatibility bounded-wait, cancellation and restart-reconciliation tests across all three projections. W4 adds only S2 observation coverage that was not part of the hidden-foundation gate: result TTL and status/`updatedAt` polling. Any regression in the already-required S1 evidence sends the change back to its introducing slice.

- [ ] Add a fixture-driven Rust parity runner that loads the immutable reviewed v0.12.3 capability oracle plus all seven shards. For every mapped/absorbed `(legacyTool, legacyVariant)` case it verifies the bound legacy request/normalized observation, dispatches the approved V13 successor through the hidden canonical handler and compares the same domain-specific typed result or side effect. Transport-replaced and removed variants execute their typed projection/rejection evidence. Genuinely new V13 capability cases execute separately and must be absent from the legacy oracle. Each lane owns its mode vocabulary and assertion function; the aggregate fails on an unknown mode, a self-declared successor that differs from the oracle, an unconsumed case, duplicate execution or a case that only passed Python shape validation.

- [ ] Close every parity inventory disposition and set `complete: true`. The aggregate test must account for all 74 baseline names, every oracle capability variant within them, every explicitly new V13 capability, all 8 successors, all 6 legacy runtime job names, executable coverage of every one of the exact 13 `run` operations, zero unowned cases, successful execution of every caseId and the measured apply registry. Structural `complete: true` without the semantic runner remains NO-GO.

- [ ] Because task observation is now implemented, create a new active successor/evidence `DEC.*` that supersedes planned `DEC.2026-08-28.V0-13-TASK-OBSERVATION-SLICE`, names the realized tests and updates the derived long-work contract. Do not edit the merged planned decision body.

- [ ] Run:

```bash
cargo test -p unica-coder v13_ -- --nocapture
cargo test -p unica-coder daemon_ -- --nocapture
cargo test -p unica-coder v13_domain_parity_all_complete_cases_execute -- --nocapture
.build/v013-plan-venv/bin/python tests/ci/test_v013_surface_profiles.py -v
.build/v013-plan-venv/bin/python tests/ci/test_v013_parity_inventory.py -v
python3 scripts/arch/registry.py --check
python3 scripts/arch/immutability.py --base origin/main
```

- [ ] Merge only when the exact 8/11 surface and one-execution invariant pass on all targets while package-selected production remains V12.

## Task 11: Prepare the Atomic Skill Routing Migration (W4)

**Owners:** Workers A/B/C own only their assigned skill patches; integrator exclusively owns the aggregate fixture/test, immutable provenance review and staged index delta

**Files:**

- Create: `tests/fixtures/migration/v0.13-skill-routing.json`
- Create: `tests/fixtures/migration/v0.13-provenance-scope-lock.json`
- Create: `tests/fixtures/migration/v0.13-skill-patches/worker-a.patch`
- Create: `tests/fixtures/migration/v0.13-skill-patches/worker-b.patch`
- Create: `tests/fixtures/migration/v0.13-skill-patches/worker-c.patch`
- Create: `tests/fixtures/migration/v0.13-skill-patches/skill-upstreams.patch`
- Create: `tests/ci/test_v013_skill_routing.py`
- Create: `tests/ci/test_v013_provenance_scope_lock.py`
- Create after both evidence validators exist: an active successor under `arch/decisions/`
- Create with that successor: `arch/invariants/INV.CI.V0-13-MIGRATION-EVIDENCE-GATE.md`
- Create with that successor: `arch/invariants/INV.CI.V0-13-PROVENANCE-SCOPE-LOCK.md`
- Generate: `arch/index.md`
- Read: `docs/provenance/reviews/2026-08-28-v013-skill-routing-upstream-review.json`
- Prepare but do not merge before G6: reviewed delta for `docs/provenance/skill-upstreams.json`
- Prepare but do not merge before G6: `plugins/unica/skills/*/SKILL.md`

- [ ] Add a failing test that enumerates all 73 tracked skill files and requires exactly one routing disposition per skill: one of the eight V13 tools plus an optional typed `apply` or `run` operation.

- [ ] Record an explicit `owner` for every skill in the routing manifest. Seed the assignment once with deterministic longest-processing-time scheduling: sort tracked `SKILL.md` files by descending line count, break ties by repository-relative path, and assign the next file to the worker with the smallest accumulated line count (then A/B/C as tie-breaker). Freeze that owner mapping before content edits; do not recompute it from modified files. On the current 73-file snapshot this yields 23/25/25 files and approximately 3546/3590/3587 source lines instead of the previous 5917/2717/2089 split.

- [ ] Before authoring any skill patch, verify that live upstream target/diff still matches the immutable Task 2A review. If it moved, stop authoring and create a newly dated reviewed successor artifact through the W0.5 classification gate. Do not mutate the accepted review or discover behavior/tool work after W1/W3 closure.

- [ ] Add assertions that V13 routing contains no direct packaged-script path where a native entry exists, no removed public tool, no public engine-delivery operation and no claim that a prepared patch is already shipped.

- [ ] Each worker prepares one tracked patch artifact for its frozen range and runs the routing/content test against a temporary applied tree. The integrator alone prepares the tracked `skill-upstreams.patch`, composes all four artifacts and commits their content-addressed manifest while the production skill/index files remain untouched. Local branch or commit SHAs are not durable evidence.

- [ ] Make `v0.13-provenance-scope-lock.json` the complete G6 input contract. It binds the immutable review path and SHA-256; upstream repository, target commit and diff digest; a closed per-entry decision/evidence set exactly equal to that review's `affectedEntries`; the role, tracked regular-file path and SHA-256 of the three worker patches plus the index patch; base blob digests for every touched path; and expected post-application blob digests. The observed 41 entries are a dated W0.5 baseline, not a hard-coded future cardinality. Require all 73 skills to be assigned exactly once, all patch roles exactly once, no duplicate/out-of-scope target and no symlink.

- [ ] Add a failing scope-lock validator that reconstructs a clean temporary tree from the recorded base blobs, verifies every digest and decision, applies the four exact patches, rejects conflicts or extra paths, compares every resulting blob with the manifest and runs routing plus upstream/provenance checks on that tree. In `--require-applied-tree` mode it additionally refreshes the live upstream report and fails unless `upstreamDrift` is exactly false and `affectedEntries` is exactly empty; the current checker exit code alone is insufficient. A manifest that merely says `--validate-only` succeeded against the current index is a false green and must fail.

- [ ] Once the catalog/oracle and provenance validators both exist, create an active successor to planned `DEC.2026-08-28.V0-13-MIGRATION-EVIDENCE-GATE`. In the same PR establish the two named `INV.CI.*` records above, each pointing to its exact falsifying test, supersede only the planned decision lifecycle fields, and regenerate the registry. S2 must cite these active records rather than plan prose.

- [ ] Run the aggregate mapping test on the unmodified production skills plus mapping fixture, then on the temporary G6 patch tree:

```bash
.build/v013-plan-venv/bin/python tests/ci/test_v013_skill_routing.py -v
.build/v013-plan-venv/bin/python tests/ci/test_v013_provenance_scope_lock.py -v
```

- [ ] Integrator records the committed manifest digest—not mutable local patch SHAs—in the G6 execution note. No child PR is opened against #631 or another open PR.

## Task 12: Prove Hidden V13 Integration Readiness (W5 / S2)

**Owner:** Integrator; three workers perform independent review roles

**Files:**

- Modify: existing injected in-process and daemon wire tests
- Create: `tests/fixtures/migration/v0.13-rc-acceptance.json`
- Create: `tests/ci/v013_package_acceptance.py`
- Modify only in G6 when assertions become executable: `tests/ci/test_v013_migration_acceptance.py`
- Modify in an independent pre-G6 provenance PR from `main`: `scripts/ci/check-attributions.py`
- Modify in that provenance PR: `tests/ci/test_attributions.py`
- Correct in that provenance PR: `plugins/unica/ATTRIBUTIONS.md`

- [ ] Exercise all eight handlers through the existing injected V13 application and canonical daemon wire harness. Do not add a public/runtime selection switch.

- [ ] Prove actor/root/revision isolation, exact SharedWork, invocation handoff, direct/Task byte-identical canonical result, cancellation, restart terminalization and no fallback/replay.

- [ ] Prove current bootstrap/core-first packaging still behaves exactly as V12 before cutover. A change here before a failing acceptance test is scope expansion.

- [ ] Before S2 exit, land an independent provenance-guardrail PR from current `main`: first add a RED test proving that every bundled tool version/commit printed in `ATTRIBUTIONS.md` matches `plugins/unica/third-party/tools.lock.json`, then correct the pre-existing `v8-runner` attribution (`ad72f642...` versus locked `7ce1b062...`) and run `tests/ci/test_attributions.py` plus `scripts/ci/check-attributions.py`. Do not fold this pre-existing defect into #631 or a handler PR; G6 may not start while package attribution and the immutable tool lock disagree.

- [ ] Immediately before S2 exit, refresh the upstream report and run the content-addressed scope-lock validator. Prove that target/diff bytes are the ones classified in the immutable review, every per-entry decision has evidence, no tool/behavior gap is unresolved, and the four recorded patches produce the exact expected temporary tree. Because production skills intentionally remain V12 until G6, `upstreamDrift=true` is not itself an S2 failure; claiming `affectedEntries: []` before applying the reviewed delta would be a false green. The clean `affectedEntries: []`/`upstreamDrift: false` gate belongs to G6 after atomic application.

- [ ] Treat S2 as revocable. If upstream moves after S2, routing/prose-only drift requires a newly dated immutable review, rebuilt tracked patches/manifest and repeated W4/W5 review. If classification reveals a bundled-tool or product-behavior gap, revoke S2 explicitly, return to the owning toolchain/W1/W3 slice, then repeat W4 and W5. G6 is prohibited until the replacement evidence restores S2.

- [ ] Define the RC acceptance fixture with exact expected 8/11 profiles, absent 74 legacy names, cold-list/no-engine requirement, one Task/one delivery, offline prefetch closure, cache version separation, fresh install, upgrade and rollback cases.

- [ ] Implement a reusable package-acceptance probe whose own parser/process/deadline unit tests pass under V12. Keep V13 package assertions disabled by explicit fixture phase `requiresCutover: true`; do not merge a red CI suite or invent a hidden package switch.

- [ ] Run the hidden aggregate gate:

```bash
cargo test --workspace --all-targets --all-features
.build/v013-plan-venv/bin/python -m unittest discover -s tests/ci -p 'test_*.py' -v
.build/v013-plan-venv/bin/python -m unittest discover -s tests/arch -p 'test_*.py' -v
.build/v013-plan-venv/bin/python tests/ci/test_v013_provenance_scope_lock.py -v
python3 scripts/arch/registry.py --check
python3 scripts/arch/immutability.py --base origin/main
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
```

- [ ] Obtain three independent reviews: architecture/layering, task/runtime semantics and migration/package evidence. Exit state S2 requires all findings resolved and all hidden suites green on macOS, Linux and Windows.

## Task 13: Prepare the Atomic RC Cutover (G6 / pre-S3)

**Owner:** Integrator in a separate PR from current `main`

**Files:**

- Modify: package version sources selected by `scripts/ci/check-version-contract.py`
- Modify: `crates/unica-coder/src/application/mod.rs`
- Modify: `crates/unica-coder/src/application/tool_contracts.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/mod.rs`
- Modify: `crates/unica-coder/src/infrastructure/daemon/server.rs`
- Modify: `crates/unica-coder/src/interfaces/mcp.rs`
- Modify: `plugins/unica/.mcp.json`
- Modify: `plugins/unica/.codex-plugin/plugin.json`
- Modify: `plugins/unica/.claude-plugin/plugin.json`
- Modify: `plugins/unica/skills/*/SKILL.md`
- Modify: `docs/provenance/skill-upstreams.json`
- Read: immutable reviewed `docs/provenance/reviews/2026-08-28-v013-skill-routing-upstream-review.json`
- Read: `tests/fixtures/migration/v0.13-provenance-scope-lock.json`
- Read: exact tracked patches under `tests/fixtures/migration/v0.13-skill-patches/`
- Create only if upstream moved since W0.5: a newly dated immutable successor review
- Modify when the reviewed source/license notice changed: `plugins/unica/ATTRIBUTIONS.md`
- Modify: `crates/unica-bootstrap/src/`
- Modify: `scripts/ci/package-unica-plugin.py`
- Modify: `.github/workflows/unica-plugin-release.yml`
- Create: `tests/ci/test_v013_migration_acceptance.py`
- Create: an active cutover successor under `arch/decisions/`
- Create: replacement/successor surface, routing, hidden-service and runtime-job records under `arch/invariants/` and `arch/contracts/`
- Generate: `arch/index.md`

- [ ] Start a fresh RC branch from current `main`; set the internally consistent prerelease version to `0.13.0-rc.1`. Verify that upstream still matches the W0.5 immutable review. If it moved, do not reclassify inside G6: revoke S2 and follow the Task 12 routing/prose or toolchain/W1/W3 return path. Apply the four exact tracked patch artifacts only after verifying their manifest digests; reject any base-blob mismatch. Regenerate the staged live report and require `affectedEntries: []` plus `upstreamDrift: false` after application.

- [ ] Add the package RED test before switching production. It must build/stage the package and fail because package-selected stdio still exposes V12.

- [ ] Before changing production routing, add a failing architecture/surface test and create an active cutover successor decision. It supersedes planned `DEC.2026-08-23.V0-13-EXECUTION-SURFACE`, names the package/profile tests as realization evidence and carries forward every unchanged daemon rule.

- [ ] In the same cutover change, create registry-supported successor records for `INV.WIRE.SURFACE-RELEASE-ROUTING` and `CTR.WIRE.TOOL-SURFACE`, plus replacements for any active hidden-service or runtime-job record whose text still requires V12 helpers or public `unica.runtime.job.*`. The new records must state production V13/8-or-11 truth and name checks; accepted product-record bodies are not edited in place or left active with contradictory wording.

- [ ] Atomically switch package-selected surface and stdio routing to V13 daemon, remove legacy registrations and six runtime-job schemas, and update bootstrap verification to the exact negotiated 8/11 profile.

- [ ] Build a staged RC package and prove from a fresh isolated cache:

  - cold `tools/list` needs core but no engine;
  - native capability yields exactly 8 names;
  - compatibility yields exactly 11 names;
  - none of the 74 baseline names remains public;
  - a cold controlled-long call creates one Task and one exact delivery;
  - `prefetch` fills the offline closure and the same call succeeds offline;
  - V12 and RC caches cannot be confused;
  - upgrade and rollback both restore the expected surface and executable.

- [ ] Run version, manifest, package and migration gates:

```bash
python3 scripts/ci/check-version-contract.py
.build/v013-plan-venv/bin/python tests/ci/test_v013_surface_profiles.py -v
.build/v013-plan-venv/bin/python tests/ci/test_v013_skill_routing.py -v
.build/v013-plan-venv/bin/python tests/ci/test_v013_provenance_scope_lock.py --require-applied-tree . -v
.build/v013-plan-venv/bin/python tests/ci/test_v013_migration_acceptance.py -v
python3 scripts/ci/check-skill-upstreams.py --check --format json
cargo test --workspace --all-targets --all-features
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
```

- [ ] Obtain independent semantic and package review and require macOS/Linux/Windows staged package evidence. Merge only after the RC matrix passes.

- [ ] Stop before tag or prerelease publication and request explicit owner approval. The merged RC commit is not evidence that public assets or marketplace state changed.

## Task 14: Publish and Validate RC, Then Release Stable v0.13 (S3 → G7 / S4)

**Owner:** Release integrator with explicit owner approvals

**Files:**

- Read first: `docs/release-runbook.md`
- Modify in a separate version-only PR: version sources selected by `scripts/ci/check-version-contract.py`
- Modify only when required by release evidence: release notes and immutable asset metadata

- [ ] After approval, publish immutable `0.13.0-rc.1` assets through the release workflow. Do not manually replace an existing asset.

- [ ] Run the fresh/upgrade/rollback/offline matrix on macOS, Linux and Windows with both native and compatibility clients. Record exact asset digests and wire probe outputs.

- [ ] Enter S3 only after the immutable published RC assets—not the staged G6 build—pass the exact 8/11 three-host wire matrix. Attach those published-asset probes to issue #581.

- [ ] Hold the RC soak until no blocker remains in daemon lifecycle, durable Tasks, engine delivery, actor isolation, migration or host packaging. A green source CI alone is insufficient.

- [ ] Open a separate version-only PR for `0.13.0`, run version consistency and full gates, and verify the diff contains no semantic code or surface change.

- [ ] After explicit approval, create the signed stable tag and let the pipeline build immutable assets. Execute runbook stage, fresh/upgrade verification and marketplace promote in order.

- [ ] Re-query the published package from Codex and Claude Code installations. Prove exact 8/11 negotiation, no legacy names, one Task/one execution and successful offline prefetched operation.

- [ ] Update issue #581 with S4 evidence and close it only after marketplace and host proofs are attached. Report separately any deferred v0.14 work (`tests`, `features`, `log`, gRPC/platform 8.5).

## Stop Conditions

Stop the affected slice and return to integrator/architecture review when any of the following occurs:

- a writer cannot prepare the complete batch before publication;
- transaction participants expand beyond Source and WorkspaceCache;
- a handler performs ambient filesystem reads after actor admission;
- an apply operation does not map to exactly one accepted registry entry;
- a legacy disposition or expected result cannot be grounded in the reviewed immutable v0.12.3 capability oracle;
- parity appears to require a legacy public schema or alias;
- upstream target/diff changes after W0.5 or review discovers an unscoped bundled-tool/product behavior gap; if S2 was already reached, revoke it and repeat the owning implementation plus W4/W5 gates before G6;
- restart recovery requires raw arguments, secrets, command lines or blind mutation replay;
- provider sharing mixes actor, root or revision identity;
- the 7 000 ms contract can only be tested with real sleep;
- a worker must edit a shared hotspot;
- package-selected V12 changes before G6;
- the G6 profile differs from exact 8/11;
- a second public MCP server or independently shipped executable becomes necessary;
- the RC cache cannot be distinguished from v0.12.3;
- `ATTRIBUTIONS.md` names a bundled tool version or commit that differs from `tools.lock.json`;
- an architectural contract changes without a successor decision and derived record.

## Completion Definition

Migration is complete only at S4, when the stable published package—not only source tests—proves the target architecture on all supported hosts. S1 and S2 are internal readiness states; S3 is an RC state. “Daemon exists”, “hidden tests pass” and “CI is green” are necessary evidence but are not synonyms for completed v0.13 migration.
