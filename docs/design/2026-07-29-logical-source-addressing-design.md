# Логическая адресация исходников 1С и низкоуровневый ресурсный обход

- Date: `2026-07-29`
- Status: `draft`
- Decision: `ADR-0020`

## Результат проектирования

Публичная граница MCP должна выбирать существующий объект метаданных или модуль
не путём к файлу, а парой из имени набора исходников и логического адреса 1С.
Один и тот же адрес должен работать для платформенного XML, EDT, CF/CFE,
файловой базы и автономного сервера, когда у Unica появятся соответствующие
поставщики.

Для этого достаточно небольшого общего ядра:

- `sourceSet` выбирает источник и принадлежащий ему корень метаданных;
- `MetadataAddress` повторяет внутреннее платформенное имя относительно этого
  корня, а не раскладку выгрузки;
- один тип адреса охватывает объекты метаданных и прикреплённые к ним модули,
  хотя модуль не объявляется объектом метаданных;
- версия платформы определяет доступное дерево типов, дочерних коллекций и
  модулей;
- поставщик разрешает адрес в собственные закрытые ресурсы;
- MCP возвращает только логические адреса и непрозрачные идентификаторы
  ресурсов, а не внутренние файловые пути поставщика.

Помимо высокоуровневых инструментов нужен ограниченный ресурсный обход. Он
позволит ИИ получить все исходные ресурсы, которыми владеет объект или модуль,
прочитать их и, когда поставщик явно разрешает это, безопасно заменить
существующий текст. Такой обход закрывает временные пробелы в возможностях
Unica, не превращая MCP в произвольный файловый редактор.

## Почему текущей адресации недостаточно

Сейчас одна и та же сущность описывается в разных инструментах через
`sourceDir`, `path`, `moduleHint`, `ObjectPath`, `FormPath`, `TemplatePath`,
`RightsPath`, `ExtensionPath`, `ConfigPath` и другие варианты физических путей.
Из-за этого:

1. ИИ должен знать раскладку выгрузки конфигуратора до того, как сможет вызвать
   предметный инструмент.
2. Один объект имеет разные внешние идентификаторы в платформенном XML, EDT,
   архиве CF/CFE и работающей информационной базе.
3. Результаты поиска по коду нельзя без дополнительного перевода передать в
   диагностику или мутацию.
4. Поддержка расширений, внешних обработок и внешних отчётов размножает
   файловые соглашения в публичных схемах.
5. Будущий поставщик без доступной файловой системы вынужден был бы изображать
   путь, которого у него нет.

ADR-0015 сознательно ограничил `unica.code.patch` одним модулем конфигурации в
платформенной XML-выгрузке. Задача #260 требует расширения на расширения, а
обсуждение добавило внешние обработки и внешние отчёты. Простое добавление ещё
трёх вариантов файловой раскладки исправило бы только ближайший симптом и
закрепило бы неверную границу.

## Что уже определила платформа

Синтакс-помощник установленной платформы `8.3.27.2074` и документация
платформы дают готовую модель, которую не следует заменять собственным деревом.

1. Глобальное свойство `Метаданные` имеет тип
   `ОбъектМетаданныхКонфигурация`. Оно предоставляет свойства-коллекции
   `Документы`, `Справочники`, `ОбщиеМодули` и другие. Элемент такой коллекции
   является конкретным `ОбъектМетаданных:*`, а его дочерние объекты доступны
   через собственные коллекции. Метод `Родитель()` ведёт обратно к владельцу.
2. Адресуемые объекты метаданных предоставляют `ПолноеИмя()`, а корень —
   `НайтиПоПолномуИмени()`. Платформа тем самым уже владеет типами узлов,
   отношениями родитель—ребёнок и именами; эти сведения не нужно выводить из
   имён каталогов выгрузки.
3. Модули представлены рядом с метаданными, но объектами метаданных не
   являются. Например, свойство
   `Метаданные.МодульУправляемогоПриложения` всегда возвращает
   `Неопределено`: встроенный язык не умеет работать со значением модуля.
   Поэтому модуль нельзя выдавать за результат
   `Метаданные.НайтиПоПолномуИмени()`.
4. У платформы уже есть имена модулей, используемые отладчиком и
   технологическим журналом:
   `ManagedApplicationModule.Module`,
   `CommonModule.Global.Module`,
   `Catalog.Counterparties.ObjectModule` и
   `DataProcessor.DataProcessor1.Form.Form1.Form`. Последний пример особенно
   важен: платформенный сегмент — `Form`, а терминальный сегмент модуля формы —
   тоже `Form`; это не файловые `Forms` и `FormModule`.
5. Встроенный метод `ПолучитьФорму()` использует пути
   `Справочник.Контрагенты.Форма.ФормаОбъекта` и
   `ОбщаяФорма.ФормаСохраненияФайла`. Для внешних артефактов он определяет
   отдельные пространства
   `ВнешняяОбработка.<имя>.Форма.<имя>` и
   `ВнешнийОтчет.<имя>.Форма.<имя>`.
6. Внешняя обработка и внешний отчёт не становятся детьми глобального объекта
   `Метаданные`. Загруженный артефакт возвращает собственный корневой объект
   через `ВнешняяОбработка.Метаданные()` или
   `ВнешнийОтчет.Метаданные()`.
7. Для объектов расширения платформа предоставляет
   `РасширениеКонфигурации()`: метод возвращает расширение, которым объект был
   добавлен, а для основной конфигурации — `Неопределено`. Поэтому
   source-specific представление расширения можно проверять по платформенной
   принадлежности, а не только по совпавшему имени.

В Синтакс-помощнике `8.3.27` у
`ОбъектМетаданныхКонфигурация` уже 49 свойств типа
`КоллекцияОбъектовМетаданных`. Ранее записанные в проекте 45 корневых видов —
это инвентаризация каталогов одного формата выгрузки, а не дерево платформы.
Фиксировать её как грамматику `MetadataAddress` было бы категориальной ошибкой.

В каталоге описано 69 конкретных типов `ОбъектМетаданных:*`, но
`НайтиПоПолномуИмени()` охватывает только 68: `МетодHTTPСервиса` не имеет
`ПолноеИмя()` и собственного глобального адреса. Значит, даже тип с таким
названием не автоматически является адресуемой целью. Схема должна отдельно
фиксировать адресуемость, а обход — показывать неадресуемый элемент без
вымышленного `metadataPath`.

Есть и дефект самой документации: страницы `ПолноеИмя()` для подчинённых типов
повторяют старую фразу о единственной точке, тогда как
`НайтиПоПолномуИмени()` перечисляет подчинённые типы, а `ПолучитьФорму()` и
отладчик используют многоуровневые пути. Из этого нельзя честно вывести
собственную универсальную грамматику. Источником допустимого дерева,
адресуемости и имён должен быть версионированный профиль платформы и
фактическая структура выбранного источника; неизвестное сочетание отклоняется.

Исследование сверено с локальным `shcntx_ru.hbk` платформы `8.3.27.2074`,
официальной методикой
[работы с объектами метаданных](https://its.1c.ru/db/content/metod8dev/src/developers/platform/metod/other/i8102318.htm),
[именами модулей отладчика и технологического журнала](https://kb.1ci.com/1C_Enterprise_Platform/Guides/Administrator_Guides/1C_Enterprise_8.3.27_Administrator_Guide/Appendix_3._Description_and_location_of_internal_files/3.24._logcfg.xml/3.24.2._Configuration_file_structure/?language=en),
[форматами выгрузки 8.3.27](https://kb.1ci.com/1C_Enterprise_Platform/Guides/Developer_Guides/1C_Enterprise_8.3.27_Developer_Guide/Chapter_2._Managing_configurations/2.17._Dumping_configurations_to_files_Restoring_configurations_from_files/2.17.3._Export_formats/2.17.3.1._Hierarchical_export_format/?language=en)
и
[описанием внешних обработок и отчётов 8.3.27](https://kb.1ci.com/1C_Enterprise_Platform/Guides/Developer_Guides/1C_Enterprise_8.3.27_Developer_Guide/Chapter_5._Configuration_objects/5.12._Reports_and_data_processors/5.12.2._External_data_processors_and_reports/5.12.2.1._General_information/?language=en).

## Рассмотренные варианты

### Оставить пути с переводчиком только перед `code.patch`

Плюс варианта — минимальный локальный дифф. Но поиск, диагностика, граф и
метаданные продолжили бы возвращать пути, а каждый следующий поставщик снова
потребовал бы отдельного внешнего соглашения. Вариант отвергнут: он меняет
место, где проявляется зависимость, но не убирает её из MCP.

### Перенести целиком архитектуру из PR #210

PR #210 содержит полезные идеи: именованные наборы исходников, точное
определение формата, неизменяемую сессию чтения с ревизией, закрытые ручки
поставщика, раздельные возможности чтения и записи, типизированную полноту и
отказ при двусмысленности. Их нужно сохранить.

Целиком переносить решение не следует. Публичный `ObjectRef` в нём опирается на
непрозрачный `objectKey`, адреса метаданных и модулей отсутствуют, а один PR
одновременно вводит закрытую семантическую вселенную, новые крейты, полный
адаптерный конвейер и перенос существующих writer-ов. Это меняет порядок
проектирования: сначала фиксируется крупная реализация, а затем под неё
приходится придумывать адресный контракт.

### Ввести небольшую адресную модель и оставить предметные writer-ы на месте

Выбран этот вариант. Сначала фиксируется публичная идентичность и узкая граница
поставщика. Платформенный XML получает первый адаптер поверх существующих
обработчиков. Новые крейты, перенос всех writer-ов и общий семантический граф не
нужны, пока второй реальный поставщик не докажет необходимость такого
разделения.

## Идентичность

Полный адрес цели всегда состоит из двух частей:

```text
SourceAddress = sourceSet + MetadataAddress
```

`sourceSet` — имя из `unica.project.map`, а не путь. Оно обязательно для точных
операций: объект с одним именем может одновременно существовать в основной
конфигурации и нескольких расширениях. Выбор набора «по умолчанию» допустим для
обзора проекта, но не для чтения или изменения конкретной цели.

`sourceSet` одновременно выбирает корень:

- `Configuration` — объект `Метаданные` конфигурации;
- `Extension` — изолированную проекцию метаданных именно этого расширения, а не
  слитое runtime-представление конфигурации со всеми расширениями;
- `ExternalProcessor` — объект, возвращаемый
  `ВнешняяОбработка.Метаданные()`;
- `ExternalReport` — объект, возвращаемый `ВнешнийОтчет.Метаданные()`.

Поэтому литерал `Configuration` или `Метаданные` не добавляется в каждый адрес:
корень уже однозначно выбран `sourceSet`. Запрос детей без `metadataPath`
обозначает сам корень. Это также устраняет ложное утверждение, будто внешняя
обработка находится внутри глобальных метаданных конфигурации.

Модуль не образует второй публичный вид идентичности. В домене и MCP остаются
один тип `MetadataAddress` и одно поле `metadataPath`, но тип означает
«адрес в платформенном пространстве метаданных», а не «обязательно
`ОбъектМетаданных`». Результат разрешения отдельно сообщает `targetKind`:
`metadataObject` или `module`. Предметный инструмент проверяет вид цели после
разрешения: например, `unica.code.patch` требует `module`, а
`unica.meta.info` — `metadataObject`. `targetKind` выводится из адреса и не
участвует в идентичности как пользовательский селектор.

Логический адрес — основной переносимый идентификатор. Устойчивый UUID объекта
метаданных и ревизия источника возвращаются как доказательства разрешения, но
не заменяют адрес публичным непрозрачным ключом. Переименование меняет
человекочитаемый адрес; UUID позволяет распознать это как переименование в
результате операции или диагностики.

## `MetadataAddress`

`MetadataAddress` — непустое внутреннее имя платформы с сегментами через точку.
Синтаксис строки фиксирует только безопасное представление идентификаторов;
семантика переходов берётся из схемы метаданных выбранной версии платформы.
Проект не вводит собственную грамматику
`<MetadataKind>.<Name>(.<ChildKind>.<Name>)*` и не считает каталоги XML
типами платформенного дерева.

Нужно различать две платформенные формы. Выражение встроенного языка
`Метаданные.Документы.ЗаказКлиента` проходит через корень и
свойство-коллекцию; внутреннее имя объекта — `Документ.ЗаказКлиента`.
Аналогично `Формы`/`Forms` является именем коллекции, а полный путь формы и
модуля использует `Форма`/`Form`. `MetadataAddress` хранит вторую форму, а
`unica.source.children` отдельно показывает переходы через коллекции.

Примеры:

```text
Документ.ЗаказКлиента
Документ.ЗаказКлиента.Форма.ФормаСписка
ОбщийМодуль.ОбщегоНазначения.Модуль
Документ.ЗаказКлиента.МодульОбъекта
Документ.ЗаказКлиента.МодульМенеджера
Документ.ЗаказКлиента.Форма.ФормаСписка.Форма
МодульУправляемогоПриложения.Модуль
ВнешняяОбработка.Загрузка.Форма.Основная
ВнешнийОтчет.Продажи.Форма.Основная
```

Русские примеры имеют английские платформенные эквиваленты, например
`Document.ЗаказКлиента.ObjectModule` и
`Document.ЗаказКлиента.Form.ФормаСписка.Form`. Оба написания разбираются в
одинаковые типизированные сегменты; каноническое написание результата
определяется вариантом имён выбранного `sourceSet`.

Количество адресов заранее не ограничено: имена задаёт прикладная конфигурация,
а состав типов меняется между версиями платформы. Поставщик публикует
`platformVersion`, `addressDialect` и поддерживаемую схему. Схема включает
корневые коллекции, допустимые дочерние коллекции, признак адресуемости их
элементов, прикреплённые модули и русские/английские платформенные
псевдонимы. Она версионируется как данные и проверяется фикстурами, а не
дублируется перечислениями в JSON-схемах инструментов.

Элементы управляемой формы не становятся `MetadataAddress`. Они принадлежат
внутренней модели формы и выбираются предметными идентификаторами
`unica.form.*`. Аналогично, связи подсистем не кодируются в адресе объекта:
адрес описывает идентичность, а не все отношения метаданных.

## Модули внутри `MetadataAddress`

Модуль — прикреплённый исходный ресурс корня или объекта метаданных, но не
`ОбъектМетаданных`. Он включён в `MetadataAddress` осознанно, чтобы один адрес
проходил через поиск, диагностику и изменение. Резолвер при этом не вызывает
для модуля `НайтиПоПолномуИмени()`: он разбирает платформенное имя модуля и
проверяет владельца и допустимый вид модуля по схеме.

Примеры:

```text
ОбщийМодуль.ОбщегоНазначения.Модуль
Документ.ЗаказКлиента.МодульОбъекта
Документ.ЗаказКлиента.МодульМенеджера
Документ.ЗаказКлиента.Форма.ФормаСписка.Форма
МодульУправляемогоПриложения.Модуль
```

Существующий внутренний `ModuleRole` в `code.rs` описывает 11 распознанных
файловых раскладок адаптера. Он не является платформенным реестром имён:
например, адаптер называет модуль формы `FormModule`, а платформа —
`...Form.<имя>.Form`; корневой файл `OrdinaryApplicationModule.bsl` также
нельзя автоматически объявить именем отладчика. При реализации файловые роли
сопоставляются с платформенными адресами отдельной проверяемой таблицей.

Есть и скрытая коллизия: текущий `nested_module_identity` для формы сохраняет
владельца верхнего уровня и роль, но не имя дочерней формы. Файловый `path`
пока различает такие цели; простая замена пути на `owner + ModuleRole` сделала
бы модули двух форм одного объекта неразличимыми. Новый резолвер обязан
сохранить полный платформенный адрес
`<владелец>.Form.<имя формы>.Form`, а не переиспользовать усечённую внутреннюю
идентичность.

Не каждый модуль допустим для каждого вида метаданных. Допустимые сочетания
задаёт версия платформенной схемы и подтверждает конкретный источник.
Наблюдаемое количество файловых раскладок — деталь адаптера платформенного XML,
а не число возможных публичных адресов.

Отдельного типа `ModuleAddress` и поля `modulePath` нет. Внутри реализации
допустим непубличный проверенный тип вроде `ResolvedModule`, если он помогает
сделать невозможным вызов BSL-операции с объектным адресом. Это доказательство
после разрешения, а не ещё один формат идентичности.

## Канонизация и знакомые имена конфигуратора

Платформа документирует русские и английские имена одних сущностей, а линейная
XML-выгрузка всегда записывает ссылки на метаданные по-английски. Делать
английский файловый вариант единственным каноническим адресом означало бы снова
подчинить MCP одному представлению источника.

Каждый `sourceSet` поэтому сообщает `addressDialect`: `ru` или `en`.
Канонический результат использует платформенные токены этого варианта, а
прикладные имена сохраняет без транслитерации. Внутренняя типизированная форма
адреса не зависит от языка. Поставщик обязан знать вариант из источника или
своего профиля; угадывание по локали процесса запрещено.

Общий резолвер понимает платформенные псевдонимы обоих вариантов. Точные
инструменты используют тот же разбор, поэтому известный полный адрес можно
передать без предварительного вызова discovery; каждый результат всё равно
возвращает каноническое написание выбранного источника. Короткое
`ОбщегоНазначения` остаётся поисковым запросом, потому что не кодирует ни вид,
ни модуль.

Разрешение имён регистронезависимо, но результат всегда возвращает написание из
дескриптора объекта. Совпадение нескольких объектов, в том числе отличающихся
только регистром, считается двусмысленностью. Оно возвращает кандидатов и не
выбирается эвристикой.

## Минимальное обнаружение адреса

Полный discovery-граф из PR #210 для адресации не нужен. Нужны две небольшие
операции.

`unica.source.resolve`:

- вход: `sourceSet`, пользовательский `query`, необязательные фильтры
  `targetKind`, `platformType` и режим `exact` или `prefix`;
- выход: ограниченный список кандидатов с каноническим `metadataPath`,
  `targetKind`, платформенным типом, UUID объекта при наличии, отображаемым
  именем и признаком совпадения;
- без нечёткой оценки, скрытой сортировки по релевантности и автоматического
  выбора при нескольких кандидатах;
- без семантических связей, графа вызовов и содержимого ресурсов.

`unica.source.children`:

- вход: `sourceSet`, необязательный точный `metadataPath` и необязательное имя
  коллекции; отсутствие адреса означает корень метаданных выбранного
  источника;
- без имени коллекции выход перечисляет свойства типа
  `КоллекцияОбъектовМетаданных` и непосредственно прикреплённые к владельцу
  модули;
- с именем коллекции выход перечисляет только непосредственные элементы этой
  коллекции с платформенным типом, локальным представлением и признаком
  адресуемости; адресуемый элемент дополнительно получает канонический
  `metadataPath`, `targetKind: metadataObject` и родительский адрес;
- ответ ограничен, продолжим курсором и всегда сообщает `completeness`;
- переход следует фактическому платформенному дереву, а не файловым каталогам
  и не семантическим ссылкам.

Коллекция является переходом в дереве, но не `ОбъектМетаданных`, поэтому ей не
присваивается фиктивный `MetadataAddress` и `targetKind`. Неадресуемому элементу
коллекции адрес тоже не изобретается; он остаётся частью агрегата ближайшего
адресуемого владельца. Именно такой двухшаговый `children` повторяет объектную
модель платформы, даёт ИИ ручной обход при неизвестном имени и позволяет
увидеть все допустимые модули владельца. `unica.project.map` дополнительно
показывает для каждого `sourceSet` формат, вид корня, версию платформы, вариант
имён, ревизию и поддерживаемые операции.

Типичный обход выглядит так:

```text
children(main)
  → коллекции: Документы, Справочники, ОбщиеМодули, ...
children(main, collection=Документы)
  → Документ.ЗаказКлиента, ...
children(main, metadataPath=Документ.ЗаказКлиента)
  → коллекции: Формы, Реквизиты, ТабличныеЧасти, ...
    модули: Документ.ЗаказКлиента.МодульОбъекта, ...
children(main,
  metadataPath=Документ.ЗаказКлиента,
  collection=Формы)
  → Документ.ЗаказКлиента.Форма.ФормаСписка, ...
```

## Ресурсный обход

Логический адрес скрывает физическую раскладку, но ИИ всё равно нужен аварийный
низкоуровневый путь, если предметного инструмента ещё нет. Его образуют три
операции.

### `unica.source.resources`

Инструмент принимает `sourceSet`, один точный адрес и область:
`self`, `aggregate` или `registrations`. Он возвращает манифест:

```json
{
  "sourceSet": "main",
  "metadataPath": "Документ.ЗаказКлиента",
  "targetKind": "metadataObject",
  "platformType": "Document",
  "revision": "opaque-revision",
  "completeness": "complete",
  "resources": [
    {
      "resourceId": "opaque-resource-id",
      "role": "descriptor",
      "relation": "primary",
      "mediaType": "application/xml",
      "size": 18420,
      "hash": "sha256:...",
      "access": {
        "read": true,
        "replace": true
      }
    }
  ],
  "nextCursor": null
}
```

`resourceId` непрозрачен, связан с ревизией и никогда не является замаскированным
путём. Поставщик может сопоставить его файлу, записи архива, объекту EDT или
виртуальному ресурсу работающей базы.

«Все связанные ресурсы» означает ограниченный агрегат владения:

- `self` — первичный ресурс самой цели;
- `aggregate` — ресурсы, которыми цель непосредственно владеет: дескрипторы,
  модули, формы, макеты и другие составные части;
- `registrations` — записи родительских коллекций и служебные регистрации,
  которые нужны для целостного изменения агрегата.

Транзитивные ссылки на типы, подсистемы, вызываемые методы и другие объекты сюда
не входят. Иначе ресурсный обход незаметно превратится в неограниченный
семантический граф. Большие ответы ограничиваются и продолжаются курсором.

Манифест всегда сообщает `completeness`: `complete`, `partial` или
`unavailable`. Поставщик не выдаёт найденное подмножество за полный агрегат.

### `unica.source.read`

Инструмент принимает `sourceSet`, `revision`, `resourceId`, смещение и
ограничение размера. Текст читается ограниченными фрагментами с явной
кодировкой, хешем и признаком окончания. Для двоичных ресурсов первый контракт
возвращает метаданные и отсутствие текстового доступа, а не безграничный
base64-поток в контекст модели.

### `unica.source.apply`

Это не универсальный редактор. Первый контракт допускает только атомарную
замену существующих текстовых ресурсов, которые поставщик пометил
`replace: true`. Вызов:

- по умолчанию работает как предпросмотр;
- требует `expectedRevision` и `expectedHash` для каждой замены;
- повторно проверяет возможности, формат, состояние поддержки и полноту
  агрегата;
- разбирает и валидирует затронутые форматы до публикации;
- публикует весь набор или не публикует ничего;
- возвращает diff, хеши до и после и новую ревизию.

При `partial` или `unavailable`, устаревшей ревизии, несовпавшем хеше,
неподдерживаемой версии или невозможности доказать атомарность применение
запрещается. Создание, удаление, переименование и запись двоичных ресурсов в
первый контракт не входят.

Такой обход намеренно уже прямой правки файла. Желание иметь ручной запасной
путь противоречит гарантии безопасной работы с CF или информационной базой,
если понимать его как безусловную запись байтов. Противоречие разрешается
явной возможностью поставщика: чтение может появиться первым, а запись
включается только после доказательства сохранности и атомарности конкретного
формата.

## Граница поставщика

Домен содержит чистые типы адресов и версионированную схему платформенного
дерева. Слой application использует два раздельных порта:

1. порт чтения выбирает корень, разрешает адреса, перечисляет детей и ресурсы и
   читает снимок;
2. порт мутации принимает ожидаемую ревизию и публикует проверенный набор
   замен.

Сессия чтения фиксирует один снимок источника и непрозрачную ревизию.
Мутационный порт перед публикацией заново подтверждает ревизию и возможности.
Полнота, недоступность и устаревание остаются типизированными результатами.

Первый инфраструктурный поставщик разрешает адреса в платформенном XML
`8.3.27` формата `2.20`. Его схема метаданных строится по зафиксированному
профилю платформы и проверяется платформенными фикстурами; физическая
раскладка лишь сопоставляется узлам этой схемы. Физические пути существуют
только внутри адаптера. Он проверяет дескриптор владельца, вид набора
исходников, формат, containment и отсутствие перехода через ссылку прежде, чем
вернуть ресурс.

Существующие предметные обработчики остаются в
`infrastructure/native_operations/`. Общий резолвер передаёт им проверенные
закрытые ручки; ради новой границы не нужно переносить все writer-ы в новый
крейт. Текущие RLM, `bsl-analyzer` и буквальный поиск также могут оставаться
файловыми внутри инфраструктуры, но их публичные результаты нормализуются в
логические адреса.

EDT, CF/CFE, файловая база и автономный сервер добавляются реализациями тех же
портов. Поставщик работающей базы получает дерево от платформенного объекта
`Метаданные`, а внешний артефакт — от собственного `Метаданные()`. Первый этап
каждого нового поставщика — чтение с точным доказательством версии и полноты.
Возможность записи проектируется отдельно после проверки сохранности;
читаемость исходника не доказывает безопасность writer-а.

## Миграция публичных инструментов

Поля, которые выбирают существующий доменный объект, заменяются логической
адресацией:

- `sourceDir` заменяется обязательным `sourceSet`;
- целевой `path` модуля и `moduleHint` заменяются `metadataPath`;
- `ObjectPath`, `FormPath`, `TemplatePath`, `RightsPath`, `SubsystemPath`,
  `CIPath`, `ExtensionPath` и `ConfigPath`, когда они выбирают существующий
  объект, заменяются `metadataPath`;
- операции CFE с основной конфигурацией и расширением получают
  `baseSourceSet` и `extensionSourceSet`;
- результаты поиска, определения, outline, диагностики и графа возвращают
  `sourceSet` вместе с каноническим адресом.

Старые файловые поля не остаются параллельными псевдонимами: два равноправных
способа выбрать одну цель снова создадут неоднозначность. Unica имеет
доверсионный публичный контракт, поэтому миграция может быть ломающей при
синхронном обновлении схем, скиллов и приёмочных тестов.

Файловый путь остаётся у аргумента, который действительно обозначает внешний
файл или место публикации, а не объект 1С:

- файл конфигурации проекта и начальный импорт;
- входной `.cf`, `.cfe`, `.epf` или `.erf`;
- JSON/DSL-полезная нагрузка, явно прочитанная из файла;
- выходной артефакт или каталог при создании нового набора исходников;
- запускаемый внешний файл.

Такие поля получают предметные имена вроде `artifactPath`, `inputPath` или
`outputDir`, а не общее `path`. Диагностический результат
`unica.project.map` может показывать физическое происхождение локального
источника, но ни один предметный инструмент не принимает его обратно как
селектор.

## `code.patch` и `cfe.patch_method`

`unica.code.patch` получает `sourceSet + metadataPath`, требует модульный
терминальный сегмент и сохраняет нынешнюю семантику точечной вставки,
предпросмотра, хешей, EOL/BOM и проверки BSL. Первый поставщик поддерживает
четыре вида набора исходников:

```text
Configuration
Extension
ExternalProcessor
ExternalReport
```

В адресе внешняя обработка называется платформенным видом
`ВнешняяОбработка`/`ExternalDataProcessor`, тогда как `ExternalProcessor`
остаётся видом набора исходников. Эти два имени нельзя смешивать в одном
перечислении. `sourceSet` внешнего артефакта выбирает его собственный корень
метаданных, а не глобальный корень конфигурации.

`unica.cfe.patch_method` остаётся отдельной операцией. Она не просто вставляет
код: ей нужно сопоставить основную конфигурацию и расширение, проверить
заимствование и сгенерировать перехватчик с аннотацией расширения. Объединение
с `code.patch` через специальную опцию сделало бы схему зависимой от вида
источника и спрятало бы отдельные предусловия. Обе операции разделяют
`MetadataAddress`, проверку модульной цели, резолвер и безопасный текстовый
снимок, но сохраняют разную предметную семантику.

## Влияние на работу ИИ

Внутренняя адресация должна упростить работу ИИ по четырём причинам:

1. один адрес переиспользуется между поиском, чтением, диагностикой и
   изменением;
2. модели не нужно запоминать файловую раскладку каждого формата;
3. ошибки называют предметную цель и доступные роли, а не отсутствующий файл;
4. переход на поставщика без файловой системы не меняет последовательность
   вызовов.

Сходство с именами конфигуратора, отладчика, сообщений платформы и
Синтакс-помощника уменьшает ещё один класс переводов: модель может использовать
увиденное имя напрямую. При этом одного строкового резолвера недостаточно.
`unica.source.children` даёт детерминированный обход от корня, а
`unica.source.resources` показывает все физические представления уже
разрешённой цели. Вместе они позволяют выполнить вручную операцию, для которой
у Unica ещё нет предметного инструмента, не раскрывая файловую раскладку.

Это инженерная гипотеза, а не доказанный процент экономии. До принятия
реализации нужен парный набор задач: одинаковые сценарии выполняются текущими
файловыми и новыми логическими контрактами. Сравниваются число неверных целей,
исправляющих вызовов, токенов и незавершённых сценариев. В набор входят
основная конфигурация, расширение, внешняя обработка, внешний отчёт и
двусмысленное короткое имя.

## Ошибки

Ошибки должны быть типизированы и не раскрывать физический путь:

- неверный синтаксис адреса;
- набор исходников не найден или имеет неподходящий вид;
- адрес не найден или двусмыслен;
- переход отсутствует в схеме метаданных выбранной версии;
- модуль недопустим для владельца;
- вариант имён источника не доказан;
- поставщик не поддерживает требуемую возможность или формат;
- манифест неполон;
- ревизия или хеш устарели;
- объект закрыт поддержкой;
- содержимое не прошло разбор или валидацию;
- атомарность или откат не могут быть доказаны.

## Этапы реализации

Один связный PR можно удержать следующими вертикальными срезами:

1. Чистый тип `MetadataAddress`, типизированные сегменты, виды разрешённой цели
   и схема метаданных платформы `8.3.27`, полученная не из каталогов XML.
2. Каталог адресов платформенного XML, точный резолвер и операции только для
   чтения: `unica.source.resolve`, `unica.source.children`,
   `unica.source.resources`, `unica.source.read`.
3. Миграция модульных инструментов и поддержка четырёх видов источников в
   `unica.code.patch`.
4. Подключение существующих инструментов метаданных, форм, DCS, MXL, ролей,
   подсистем, интерфейса, справки, макетов и поддержки к общему резолверу без
   переноса их writer-ов.
5. Ограниченный `unica.source.apply` для существующих текстовых ресурсов.
6. Синхронизация схем MCP, дескрипторов операций, скиллов, архитектурного
   реестра и приёмочных тестов.

Бюджет масштаба: без нового крейта, без полного discovery-графа, без переноса
всех writer-ов, без реализации EDT/CF/FileDB, без создания и удаления через
сырой ресурсный API.

## Проверки проекта

- Профиль платформы `8.3.27` воспроизводит корневые коллекции
  `ОбъектМетаданныхКонфигурация`, дочерние переходы, адресуемость типов и
  платформенные имена модулей, а не список каталогов выгрузки.
- Парсер делает канонический round-trip для русского и английского вариантов
  одних адресов, включая платформенные формы и корневые модули.
- Таблица сопоставления проверяет допустимые и недопустимые модули владельца и
  не выдаёт внутренний `ModuleRole` за публичное имя.
- Фикстуры платформенного XML покрывают конфигурацию, расширение, внешнюю
  обработку и внешний отчёт.
- `unica.source.children` обходит дерево от корня, возвращает только
  непосредственных детей и доказывает полноту или сообщает её ограничение.
- Резолвер отклоняет неоднозначность, неподдерживаемый формат, выход за корень,
  ссылки и несовпадение дескриптора владельца.
- Ресурсный манифест доказывает полноту агрегата и корректно продолжает
  ограниченный ответ курсором.
- Мутация отклоняет устаревшую ревизию и хеш, сохраняет незатронутые байты и
  публикует набор атомарно.
- `code.patch` сохраняет BOM, EOL, поведение селекторов, предпросмотр и
  завершающий разбор BSL на всех четырёх видах источников.
- Подменный поставщик без файловой системы доказывает, что application и
  публичные схемы не требуют путь.
- Контрактные проверки не находят целевых файловых полей и удерживают
  одинаковую поверхность в Rust, MCP и скиллах.
- Парная оценка ИИ измеряет гипотезу об упрощении, а не заменяется субъективным
  впечатлением.

## Риски и компромиссы

- Логический адрес с именем меняется при переименовании. UUID и ревизия
  компенсируют это в результатах, но не превращаются в основной непрозрачный
  селектор.
- Пять новых исходных операций увеличивают MCP-поверхность. Их назначение
  разделено: разрешение имени, обход дерева, перечисление ресурсов, чтение и
  guarded-мутация имеют разные ограничения и не сводятся к перегруженному
  `info`.
- Сырой обход позволяет сделать больше ошибок, чем предметный инструмент.
  Поэтому он не обходит поддержку, версию, полноту, валидацию и атомарную
  публикацию.
- Одновременная миграция всех целевых путей — ломающая и заметная, но
  переходный двойной контракт закрепил бы долг и усложнил ИИ сильнее.
- Первый поставщик остаётся файловым внутри. Это допустимо: цель решения —
  нейтральная публичная и application-граница, а не запрет файловой системы в
  инфраструктуре.

## Что берётся из PR #210

Сохраняются:

- именованный `sourceSet`;
- точное определение формата без выбора «ближайшей» версии;
- неизменяемая сессия чтения и непрозрачная ревизия;
- закрытые ручки и пути поставщика;
- разделение возможностей чтения и записи;
- типизированные `complete`, `partial` и `unavailable`;
- отказ при двусмысленности, обрезанном результате и неподдерживаемой версии;
- повторная проверка ревизии и возможностей перед записью;
- атомарная публикация набора изменений.

Не переносятся:

- `objectKey` как основной публичный селектор;
- закрытая семантическая вселенная до фиксации адресов;
- перенос всех существующих writer-ов в адаптер;
- новый крейт на каждого предполагаемого поставщика;
- огромный legacy-oracle и восьмиступенчатый конвейер;
- скрытая навигация внутри универсального `info`;
- полный семантический граф в рамках адресной миграции.

Рекомендация — остановить PR #210 как реализацию, сохранить его исследовательские
выводы и начать новый связный PR от `main` после одобрения этого проекта и
ADR-0020.
