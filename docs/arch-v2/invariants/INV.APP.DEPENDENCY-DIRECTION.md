---
id: INV.APP.DEPENDENCY-DIRECTION
---

# Границы ввода-вывода и обращения к application через порты

Домен не знает о вводе-выводе, в том числе через методы экземпляров
и сторонние зависимости.

Инфраструктура не вызывает application в обход портов. Нарушение ловится
стражем, а не ревью.
