---
id: INV.APP.SUPPORT-STATE
---

# Чтение узнаёт поддержку по логической цели

Канонический `view` по логическому адресу объекта или подсистемы возвращает
состояние поддержки в `props`. Для этого не нужен физический путь
к файлу состояния поддержки.
