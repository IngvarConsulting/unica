---
name: meta-compile
description: Создать минимальный объект метаданных 1С через типизированную операцию добавления. Временное имя маршрута сохраняется до редакционной миграции скиллов.
argument-hint: <sourceSet> <kind> <name>
allowed-tools:
  - Read
  - Glob
---

# /meta-compile — переходный маршрут создания метаданных

## MCP routing

- Preferred path: use MCP `unica` tool `unica.meta.add`.
- Передайте логический набор исходников `sourceSet`, один из поддерживаемых
  видов `kind` и имя `name`; физические пути и внешние файлы определений не
  являются частью контракта.
- Вызов по умолчанию строит preview. Передавайте `dryRun: false` только когда
  пользователь явно попросил применить изменение.
- Сложный объект создаётся двумя отдельными шагами: валидный минимальный объект
  через `unica.meta.add`, затем структурные изменения через `unica.meta.edit`.
- Результат и внутренняя проверка находятся в типизированном `data`; отдельный
  публичный маршрут проверки не нужен.

```json
{
  "jsonrpc": "2.0",
  "method": "tools/call",
  "params": {
    "name": "unica.meta.add",
    "arguments": {
      "sourceSet": "main",
      "kind": "Catalog",
      "name": "НовыйСправочник",
      "dryRun": true
    }
  }
}
```

Справочные таблицы старого механического формата больше не являются инструкцией
к публичному MCP. Их временные файлы сохраняются только до отдельного fact
audit и редакционной миграции пакета.
