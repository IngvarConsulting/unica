## Что меняется

<!-- Одним абзацем: какая задача решена и каким способом. Ссылка на issue. -->

## Правила продукта

<!--
Назовите затронутые гарантии и ссылки на существующие правила в arch/rules/.
Если отдельного правила нет, опишите проверяемое поведение; создавать запись
ради заполнения шаблона не нужно.
-->

- Затронутые гарантии и проверки:
- Согласованное изменение гарантии, если есть:

При изменении публичного контракта `unica.*` покажите проверку совместимости
затронутых потребителей. При изменении состава инструментов обновите
`docs/tool-surface.md` через генератор. Применимые правила читаются до выбора
решения; их формат и границы пилота описаны в `arch/README.md`.

## Проверка

```sh
cargo fmt --all -- --check
cargo clippy --workspace --all-targets --all-features -- -D warnings
cargo test --workspace -- --test-threads=1
python3.12 -m pip install -r tests/ci/requirements.txt
python3.12 -m unittest discover -s tests/ci
git diff --check
```

- [ ] Новое поведение покрыто тестом, который можно назвать по имени.
- [ ] Документация, описывающая изменённое поведение, обновлена в этом же PR.
