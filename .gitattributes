*.sh text eol=lf

# BSP parity fixtures preserve harvested bytes, including BOM and CRLF.
tests/fixtures/unica_mcp_script_parity/bsp/** -text -whitespace

# The donor parity snapshot is byte-for-byte upstream evidence, including CRLF.
tests/fixtures/unica_mcp_script_parity/cc-1c-skills/** -text -whitespace

# The 8.3.27 support fixture and its CFE module are raw platform evidence.
# Their BOM and CRLF bytes are part of the assertions and must not be normalized.
tests/fixtures/platform_8_3_27/support-edit-bin-only/** -text -whitespace
tests/fixtures/unica_mcp_script_parity/cfe-patch-method/base-common-module.bsl -text -whitespace

# Harvested platform specifications retain their original CRLF where present.
plugins/unica/references/specs/*.md whitespace=cr-at-eol

# This prompt-visible skill intentionally retains its CRLF source formatting.
plugins/unica/skills/form-patterns/SKILL.md whitespace=cr-at-eol
